# 《帮我分析》开发日志

> 记录从 0 到可实机运行的全过程：阶段交付、实机故障与根因、架构决策。
> 最后更新：2026-08-25（CST）

## 总览

| 日期 | 里程碑 | 提交 |
|---|---|---|
| 2026-07-21 上午 | 阶段 0–6 全部完成，187 测试全绿，Debug/Release 打包 | `e7efc9c`…`9d7eb7a` |
| 2026-07-21 下午 | 安装到本机，进入实机测试 | — |
| 2026-07-21 下午 | 分析模块从 OpenAI 切换到 Kimi 网关（真实联调通过） | `c2ba96e` |
| 2026-07-21 下午 | 中文语音资源安装 + App 内一键下载按钮 | `ee85b54` |
| 2026-07-21 傍晚 | 三轮卡死排查，根治视图自激死循环 | `31919f5` `0cc5a0b` `53a698c` |
| 2026-07-21 晚 | Key 分家（分析/分人独立条目）；分析超时与截断修复 | `398964d` `29ba298` |
| 2026-07-22 | GitHub 公开仓库 `zhaobo258-netizen/BWFC` 已创建 | — |
| 2026-07-24 | 阶段 A–D 全部完成；Kimi 网关迁移至 api.kimi.com + 账号登录（OAuth），360 测试全绿 | — |
| 2026-07-26 | 审计修复 H1–H4、M1–M5+L1；380 测试、媒体探针与缩短耐久全绿 | `eafadf0`…`a1f7075` |
| 2026-07-29 | 代码审计修复计划批次 1–5：Bug 1–6 + 界面 1–7，511 测试全绿（未提交） | 工作区 |
| 2026-07-29 | 批次 6 最小版：项目重命名 + 在 Finder 中显示，514 测试全绿；删除仍待授权 | `39ff4bf` |
| 2026-07-29 傍晚 | 打包安装后实机实测；老板报 5 项问题，诊断出 Kimi「未接入」真因是 Keychain service 断链 | 仅诊断，未改码 |
| 2026-07-30 | 实测反馈批次 A–D：Keychain 跨 service 迁移、录音条显示前提、↩ 发送、对话框拖放，527 测试全绿；已打包安装 | 见本节 commit |
| 2026-07-30 收尾 | 删除项目落地（守卫 + 二次确认 + 目录清理）；M4 损坏恢复演练 PASS；60 分钟完整 soak，534 测试全绿；已推送并安装 | `7124d06` |
| 2026-07-30 密码框 | 实测反复弹授权框根因：跨签名身份的历史条目，「始终允许」点一次即永久静默；现代钥匙串需 Team ID 不可行；修正假注释并收紧启动迁移，535 测试全绿 | 本轮 |
| 2026-08-25 | 动机分析深度框架：四层透视（心理/社会/谈判/沟通）+ 强制上下文融合，prompt 版本 2026-08-24.8，604 测试断言零失败 | 工作区 |

---

## 2026-07-21 上午：阶段 0–6（按计划书实施）

实施依据：`../01_帮我分析_Mac_MVP开发实施计划.md`。

- **阶段 0**：SPM 工程骨架（本机无 Xcode，仅 CLT + Swift 6.3.2，采用「SPM 可执行包 + Scripts/make_app.sh 自制 .app + ad-hoc 签名」路线）；核心模型（Meeting/Participant/TranscriptSegment/AnalysisSnapshot/Insight/TopicState）；Keychain 服务；OSLog 脱敏日志；会议列表/设置页骨架。23 测试通过。
- **阶段 1**：会前表单、参会人（≤4 人）、麦克风选择/电平测试、录音状态机（draft→ready→recording⇄paused→finalizing→completed）、异常退出恢复、回放。66 测试。
- **阶段 2**：Apple Speech `SpeechTranscriber` 本地转写（三道可用性检查、progressive preset、上下文词汇）；临时/最终片段合并去重器（时间范围为主 + 文本相似度为辅）；底部转写 UI。93 测试。
- **阶段 3**：声音样本（2–10s 校验）、20s 分片 + 2s 重叠上传队列（持久化、指数退避、上限后待用户重试）、OpenAI diarize 接口（只传 p_01… 代号）、断网排队补传、人工修订保护。141 测试。
- **阶段 4**：严格 JSON Schema 分析、原话不可信包裹（注入防护）、触发器（≥3 片段或 45s + 10s 防抖 + 串行单请求）、证据存在性校验（无证据不进 UI）、左侧 7 段结构 + 右侧六类卡片。173 测试。
- **阶段 5**：finalizing 收尾、会后页、证据回放、重新生成分析、Markdown/JSON 导出、整场删除（二次确认 + 清理验证）。187 测试。
- **阶段 6**：soak 稳定性脚本（3/4 分钟缩短版 PASS，帧零丢失、内存增长 <1MB）、交付说明.md、人工验收清单。

**环境绕行决策**：CLT 缺 SwiftData 宏 → 模型以纯 Swift 类实现 + `MeetingStoring` 协议 + JSON 原子持久化（装 Xcode 后可零字段改动切回）；CLT `swift test` 静默不执行 → `Scripts/run_tests.sh` 自建测试执行器。

## 2026-07-21 下午：安装与云端切换

- App 安装到 `~/Applications/BangWoFenXi.app`（去 quarantine、ad-hoc 签名校验）。
- **用户决策**：不用 OpenAI，改用本机 Kimi CLI 的 API。探测确认网关为 Anthropic 风格：`POST https://agent-gw.kimi.com/coding/v1/messages`，模型 `kimi-for-coding`，无 Structured Outputs 能力 → JSON 靠提示词约束 + 本地严格校验。`KimiAnalysisService` 落地，真实网关联调一次通过（3 片段 → 合法六类 JSON）。Key 写入 Keychain（`account=openai`，后迁移为 `kimi`）。
- **实机故障 1**：「中文语言资源尚未安装」。计划书写的"去系统设置下载"实际不存在该入口；用 `AssetInventory.assetInstallationRequest + downloadAndInstall` 探针直接装好（zh_CN: supported → installed），并把一键下载按钮做进 App（`ee85b54`）。

## 2026-07-21 傍晚：三轮卡死排查（关键战役）

症状：点「开始录音」后约 2 秒界面冻结，计时器停走，转写区不动。录音文件一直在正常增长（采集无恙）。

- **第一轮（31919f5）**：误判为发布风暴。临时结果 250ms 节流 + 右键菜单压平 + 可用性检查缓存。有改善但未根治。
- **第二轮（0cc5a0b）**：误判为全量替换 + 单行过贵。差分发布（无变化跳过）+ Equatable 行 + 值类型菜单数据 + **PerfCounters 调试计数条**（本次排查的转折点：让"模型层发布了多少次"在界面上可见）。仍未根治。
- **第三轮（53a698c）实锤**：计数条证明模型层几乎零发布、主线程却 100% 在 SwiftUI flushTransactions——**视图自激更新死循环**。根因：`TranscriptPanelView` 渲染行时调用 `SpeakerMapper.resolve()`（`mutating` 方法），它挂在 `@Observable` 对象的存储属性上；Swift 对 mutating 方法的任何调用都经修改访问器读改写回 → 广播变更通知 → 视图失效 → 再渲染 → 再调用……在第一个字「万」上屏瞬间引爆。修复：读路径改纯函数（编译器锁死纯度），写路径收编到模型层两处。

**教训（已写入 Agent.md）**：① @Observable 存储属性上禁止 mutating 方法被渲染路径调用；② 先仪表化再猜因；③ 采样（sample 命令）+ 界面计数条是定位 SwiftUI 卡死的两件套。

## 2026-07-21 晚：云端链路收尾

- **401 串扰（398964d）**：分人模块拿 Kimi Key 打 OpenAI 端点 → 401，横幅却提示"分析暂停"。修复：Key 分家（`kimi` / `diarization` 独立条目、互不外借、旧条目自动迁移）、401 只暂停本服务、分人未配置时灰态零请求。日志安全字段 public 化（此前全是 `<private>`，无法诊断）。
- **分析"发一次就结束"（29ba298）**：日志显示后台一直在重试但全失败：① 超时——10 分钟会议 Kimi 真实响应 ~95s，旧 60s 超时不够 → 240s；② 截断——`max_tokens` 是 thinking+text 总预算，4096 被 thinking 吃光 → 关闭 thinking（网关支持，响应快一倍）+ 预算 16384；③ 状态诚实化——失败显示「最近重试失败（原因），将自动重试」。真实联调：45 片段 ≈ 10.5 分钟会议 → 94.6s 返回完整合法分析（10 洞察 + 8 议题等，证据全有效）。

## 当前状态（截至 2026-07-22 09:51）

- 代码：60 个源文件 + 35 个测试文件，224/224 测试全绿，编译 0 警告，git 工作区干净。
- 实机已验证：录音、计时、本地转写（3 秒内上屏）、节流差分渲染、首次 Kimi 分析（议题/立场/未决/关键数字/洞察卡片 + 证据）。
- **待实机复核**：长会议（10 分钟+）分析持续多版更新（第六版修复后用户尚未回报）；导出 Markdown/JSON 的实机操作；60 分钟完整 soak（脚本就绪未跑）。
- **待决策**：说话人识别 provider（Kimi 无音频接口；选项：OpenAI Key / 讯飞 / 火山 / 保持手动标注）。
- 已知真实指标偏差：10 分钟以上会议单版分析延迟约 1.5–2 分钟（计划目标 60 秒，偏紧）。

---

## 2026-07-22：知识花园版 阶段 A——V2 数据底座与安全迁移

实施依据：`../03_帮我分析_知识花园版_产品开发文档_20260722.md`（§8 数据合同、§9.1 协议、§15.1 迁移策略）。本阶段不动 UI、不动分析 Schema、不碰 Obsidian/MCP，未提交 git。

**新增模型**（`Models/`）：`Project.swift`（schemaVersion=2，含 ProjectSourceType/ProjectScenario/ProjectStatus、NoteDocument、legacySnapshots 保留旧谈判快照；V2 通用分析快照属阶段 D）、`Speaker.swift`（legacySide 保留旧 Participant.side 原值，不进主 UI）、`ProcessingJob.swift`、`ArchiveState.swift`。`TranscriptSegment` 追加三个可选字段（speakerConfidence/languageCode/sourceAssetId），合成 Codable 自动容忍旧 JSON。

**新增持久化**（`Core/Persistence/ProjectStore.swift`）：`ProjectStoring` 协议 + `JSONProjectStore`（projects.json，与 meetings.json 同目录，iso8601+sortedKeys+原子写）+ `InMemoryProjectStore`。

**迁移器**（`Core/Persistence/ProjectMigration.swift`）：
- 纯函数 `MeetingToProjectMigrator`：状态映射 draft/ready→creating、recording→recording、paused→paused、finalizing→processing、completed→ready；原 UUID、录音相对路径、startedAt/endedAt、pauseIntervals、片段 ID/时间轴、参与者（side→legacySide）、历史快照全部保留；segments/snapshots JSON 往返深拷贝避免共享可变引用；同一输入+migratedAt 产出完全一致。
- 文件级 `ProjectMigrationCoordinator`：标记文件（projects-migration-v2.json）存在且 projects.json 可解码 → alreadyMigrated 空操作；标记在而 projects.json 损坏 → 抛 corruptedNewStore 且不动任何文件；否则 tmp 写入 → 复读逐项校验（数量/id/路径/暂停区间/片段序列/快照数）→ 原子替换（replaceItemAt/rename）→ 写标记。全程不写 meetings.json、不碰录音目录，中断残留 tmp 可安全重跑。
- `AppEnvironment` 注入 `projectStore`（默认内存实现，旧调用点零改动），新增 `allProjects()/persist(_:)`；`live()` 启动时执行一次性迁移，失败仅脱敏记录 `project_migration_failed`，不影响启动与旧数据。

**测试**：新增 2 套件 16 用例（空库、单/多项目、旧字段缺失、异常状态映射、重复迁移字节一致、新文件损坏零破坏、残留 tmp、标记缺失重跑、纯函数确定性、完整读写往返、AppEnvironment 注入等）。**240/240 全绿（42 套件），swift build 0 警告**；旧 224 用例全部保持。

**环境事故记录**：当日下午测试多次整体卡死，采样定位为登录钥匙串被锁定（休眠触发），Security 框架写操作全部阻塞在 securityd，并弹出「请输入钥匙串密码」授权框——与代码无关。重启 securityd + 用户解锁钥匙串后恢复。教训：跑测试前若钥匙串已锁，Keychain 相关用例会把整个执行器拖死。

---

## 2026-07-22：阶段 A 补强——迁移字段完整性与无标记恢复

依据 Codex 只读审计发现的两个数据安全缺口修复，仍未进入阶段 B，未提交 git。

**一、旧字段完整保留**
- 新增 `Models/LegacyMeetingMetadata.swift`（Codable/Hashable）：完整保存旧 Meeting 的 background、ourGoal、ourBottomLine、counterpartContext、glossary、audioUploadConsentAt、lastAnalyzedSegmentEndMs，作为 `Project.legacyMetadata`（可空）挂载；新建 V2 项目为 nil。
- `Speaker` 增加 `legacyVoiceReferencePath` / `legacyVoiceReferenceDurationMs`，保留旧声音样本相对路径与时长（样本文件原地不动）。
- Project 自定义解码对缺失 legacyMetadata 键的旧 JSON 回退 nil；Speaker 合成 Codable 自动容忍缺失。

**二、无标记恢复分支（消除覆盖窗口）**
`ProjectMigrationCoordinator.migrateIfNeeded` 现在分三支：
1. 标记存在 + projects.json 可解码 → alreadyMigrated；标记在而 projects.json 损坏 → corruptedNewStore 拒绝写入。
2. **标记缺失 + projects.json 已存在**：绝不覆盖——完整解码（失败即 corruptedNewStore 拒绝），逐个验证旧 Meeting 均有同 id Project（缺一即 validationFailed 并保持所有文件原样），通过后只补写标记（outcome=markerRestored），projects.json 字节不变，V2 新项目全部保留。
3. 两者皆无 → 首次迁移（tmp 写入 → 复读校验 → 原子替换 → 写标记）。

**三、真实数据演练（只读 + 临时副本）**
真实 Sandbox 目录（~/Library/Containers/com.zhaobo.BangWoFenXi/.../BangWoFenXi）只读确认 6 会议、无 projects.json；复制 meetings.json 到 /tmp 临时目录，用 swiftc 独立编译的演练程序执行迁移：6→6，id 全匹配、片段 id 序列全匹配、legacyMetadata 全覆盖；二次运行 alreadyMigrated；真实与副本 meetings.json 的 SHA-256 双向不变；未输出任何标题/正文/路径；演练后临时目录已删除。

**四、durationMs 口径**
保留字段并在 `Project.swift` 与迁移器注释中明确：durationMs = 会议墙钟时间轴（包含暂停区间），非媒体实际时长；阶段 B 播放条必须另行获取真实媒体时长，两个口径不得混用。

**测试**：迁移套件新增 5 用例（恢复保留额外 V2 项目、缺项目拒绝补标记、补写标记失败字节不变、恢复后多次重启幂等、标记缺失且 projects.json 不可解码拒绝），既有单项目/最少键用例扩充 legacyMetadata 与声音样本逐项断言，持久化套件扩充 legacyMetadata 往返与缺失回退；「标记未写重跑」用例语义升级为 markerRestored（断言强度不变）。

**既有抖动测试加固（根因修复，非弱化）**：验证中「结束会话：丢弃尾部临时片段并回到空闲」与「最终结果不受节流影响」两个既有用例在并行高负载下间歇失败（机器多用户负载、消费任务调度超过固定 100/120ms 睡眠窗口）。根因是测试用固定睡眠等待异步消费；已将这两处改为轮询等待条件达成（2 秒上限后退出循环，仍由原有断言如实判定），断言逐字未改、全部用例保留。

**结果**：`swift build` 0 警告；`Scripts/run_tests.sh` **245/245 全绿（42 套件）**；`git diff --check` 通过。

---

## 2026-07-22：GitHub 公开仓库建立

- **远端已生效**：公开仓库 [zhaobo258-netizen/BWFC](https://github.com/zhaobo258-netizen/BWFC) 已创建，仓库未归档，默认分支为 `main`。
- **现场核验状态**：截至本次检查，GitHub 返回仓库大小为 0，远端尚无项目代码。
- **本地状态**：本地 Git 仓库位于当前项目目录，分支为 `main`，HEAD 为 `060263c`；尚未配置任何 Git remote，因此本地代码和当前未提交改动均未推送到该仓库。
- **交付边界**：目前仅完成 GitHub 公开仓库创建；代码推送、远端构建/发布、客户可交付及真实使用均未验证。

---

## 2026-07-22：阶段 A 提交与推送

- 本地两个原子提交：`a1cf144`（test: 稳定异步转写测试等待）、`868c679`（feat: 建立 V2 项目数据与安全迁移底座）。
- 配置 `origin = https://github.com/zhaobo258-netizen/BWFC.git` 并 `git push -u origin main`；`git ls-remote` 核验远端 `refs/heads/main` 与本地 HEAD（868c679）完全一致，工作区干净。（上一节"仓库大小为 0 / 未配置 remote"的状态至此被本推送取代。）

---

## 2026-07-22：知识花园版 阶段 B——首页与三栏工作台

实施依据：03 号文档 §16 阶段 B。范围纪律：不做阶段 C 导入实现、不做阶段 D 通用 Schema、不接 Obsidian/MCP、不删旧 Meeting 页面与旧模型、不顺手重构无关模块。未提交、未推送。

**新首页（`Features/ProjectHome/`）**
- `ProjectHomeView`：开始录音（主按钮）/ 导入音视频（次按钮，明确标注"后续版本提供"，点击如实提示，不伪造可用状态）双入口；最近项目按 lastActivityAt 倒序（标题/状态徽标/来源/时长/活动时间/一行摘要）；未正常结束项目的非阻塞提示；首次录音一次知情确认（@AppStorage 记忆）。
- `ProjectHomeSupport`：排序与摘要纯逻辑（可单测）。
- 开始录音 → 立即创建 Project（"未命名录音 · HH:mm"）→ 直达工作台 autoStart，两次交互内开录。

**三栏工作台（`Features/ProjectWorkspace/`）**
- `ProjectWorkspaceView`：顶部（返回/可编辑标题/录音状态/处理详情弹层）+ 左文稿（复用 TranscriptPanelView）+ 中 AI 工作区（实时总结 / 动机与目的两页签，复用 StructureSummaryView 与 InsightCardListView，证据点击定位左栏高亮）+ 右笔记（TextEditor + 自动保存状态条）；底部录音条（红点/时长/标记/暂停/继续/结束）录音期间始终可达；技术状态（Key、分片、Provider）收进处理详情；异常横幅（麦克风断开、语言资源、云端暂停、未正常结束恢复）仅异常时出现。结束录音后收尾流水线后台继续，可立即返回首页。
- `ProjectRuntimeSession`：Project ⇄ 运行时 Meeting 纯函数桥接——录音/转写/分人/分析链路零改动复用（它们只认 Meeting），水合同时深拷贝，回写经 applyRuntime + ProjectStoring；状态映射与迁移器单一口径。
- `ThreeColumnLayout`：三栏拖动调宽 + @AppStorage 记忆（默认 34/41/25，最小 320/420/280）。
- `NoteController`：0.8s 防抖自动保存到 Project.note；保存失败如实显示可重试；saveNow 立即落盘（视图消失/结束录音时）；异常退出后从 projectStore 恢复。
- `AppRouter`：新增 projectHome（默认）/ projectWorkspace(id, autoStart)；旧路由与旧页面保留。

**实机验证（真实用户驱动）**：打包安装后由真实用户走通 golden path——开始录音 → 知情确认 → 直达工作台开录；7 分钟以上真实客户对话连续转写入左栏，中栏增量分析持续产出议题/立场/关键数字及证据时间戳，右栏笔记自动保存（"已自动保存 HH:mm:ss"），标题单击改名生效；1280×832 与 1440×836（本机物理最大高度）无裁切、控件可达。截图存于 `build/阶段B验证截图/`。
**实机发现并已修复**：恢复横幅在活动录音中误显示（条件缺"本会话已开录"门控）——已在源码修复（didStartSessionThisView），为避免中断用户进行中的真实录音，未当场热替换应用包。

**测试**：新增 3 套件 16 用例（桥接 6、笔记 6、首页纯逻辑 4）。笔记防抖用例在全量并行高负载下曾被饿死（独立运行全过）——waitUntil 上限提至 10s（断言不变）。`swift build` 0 警告；`Scripts/run_tests.sh` **261/261 全绿（45 套件）**。

**既有异步测试的进一步加固（根因修复，非弱化）**：阶段 A 已加固 2 处固定睡眠用例；阶段 B 验证期间，实机高负载（用户真实录音与本机编译并行）又使 `LocalTranscriptionControllerTests` 其余 4 处、`PublishDiffRegressionTests` 3 处、`RenderingStormRegressionTests` 2 处同类固定睡眠断言间歇失败（消费任务调度超过 100–500ms 固定窗口所致，与功能代码无关）。已统一改为轮询等待条件达成（5 秒上限，超时仍由原断言如实判定），断言逐字未改。教训：涉及异步流消费的测试一律用条件轮询，禁止固定睡眠。

**遗留说明**：1440×1024 高度超出本机屏幕物理可用高度（最大约 836pt @ 2x），以 1440×836 实测代替；麦克风拒绝与保存失败 edge case 由代码路径与单测覆盖，未做系统级破坏性演练。

---

## 2026-07-24：阶段 B 补强——录音资产回写缺陷修复与设置返回路由

依据 Codex 只读审计发现的缺陷补强（Kimi 完成开发，未提交；由后续维护者审计后落库）。

**一、录音资产缺失（真实数据缺陷）**
- 根因：阶段 B 初版 `ProjectRuntimeSession.applyRuntime` 未回写 `audioRelativePath`，导致部分 liveRecording 项目 `runtimeAssetRelativePath=nil`，但录音文件真实存在于标准路径 `Meetings/<projectID>/recording.caf`。
- 修复①：`applyRuntime` 增加录音路径回写（运行时为权威）。
- 修复②：新增 `Core/Persistence/ProjectAssetRepair.swift`——启动时幂等安全补写：仅当 sourceType==liveRecording、路径为空、标准路径文件存在且非空三条件全满足才补写 projects.json 中的路径字段；不移动、不改写任何录音文件；已有非空路径绝不覆盖。`AppEnvironment.live()` 挂接，失败仅脱敏记录。
- 测试：新增 `ProjectAssetRepairTests`（满足/不满足条件、幂等、不覆盖已有路径等）。

**二、设置页返回来源路由（Codex 审计补强）**
- `AppRouter` 记录进入设置前的路由（settingsReturnRoute），设置页「返回」回到来源页而非固定回旧会议列表；新增 `AppRouterAndLayoutTests`。

**三、验证状态**
- 2026-07-23 全面审计复核：`swift build` 0 警告；`Scripts/run_tests.sh` **279/279 全绿（48 套件）**；敏感数据扫描干净（无硬编码 Key、无音频入仓）；沙盒真实数据 projects.json + 迁移标记正常。
- ⚠️ 实机 `~/Applications/BangWoFenXi.app` 仍为 7/22 旧包，不含本节两项修复与恢复横幅门控修复；build/ 中 7/24 新包已就绪待安装。
- ⚠️ 审计时 GitHub 不可达（443 连接超时），本批提交尚未推送，网络恢复后需 `git push`。

---

## 2026-07-24 10:40：阶段 B 补强交接存档（致下一窗口）

> 本窗口（Kimi）因额度中止。以下状态经逐条核实，供下一窗口直接接手。

### 当前提交线（本地 main）
- `868c679` 阶段 A 数据底座（**已推送** origin/main，ls-remote 已核验一致）。
- `a1cf144`→`abbe012`→`673c980`→`dcf30c9`：阶段 B（首页/三栏/笔记/路由/隐私文案/联合三栏）+ 异步测试轮询化 + 录音资产回写修复，**全部由 Codex 窗口提交，未推送**（07-23 审计记录 GitHub 不可达；接手先查 `git status` 与网络再决定推送）。

### 本窗口在 HEAD 之上的未提交改动（仅 2 个文件，均是测试隔离修复，建议保留）
1. `BangWoFenXiTests/MeetingDeletionTests.swift`：AppEnvironment 构造补 `keychainServiceName: "com.zhaobo.BangWoFenXi.tests.meeting-deletion"`。**根因**：该既有测试未传 service 名，触达生产钥匙串条目 `com.zhaobo.BangWoFenXi.cloud-api-key`，每个新测试二进制都触发系统授权弹窗并把全量测试拖死（今日实锤一次 225 秒卡死）。此修复后弹窗消失。
2. `BangWoFenXiTests/ProjectStoreTests.swift`：fullModelRoundTrip/emptyStoreReturnsEmpty/overwriteReflectsDeletion 改为每用例独立临时目录（makeCaseDirectory），消除套件内并行写同一 projects.json 的竞态。

### 验证状态（关键，勿重复踩坑）
- Codex 记录：`swift build` 0 警告；`Scripts/run_tests.sh` 279/279 全绿（48 套件）。
- 本窗口今日两次全量跑均见 2 个笔记防抖用例失败——**已查明非代码问题**：第一次为共享目录竞态（已修），第二次为钥匙串授权弹窗冻结进程 200 秒导致 waitUntil 超时（弹窗根因已修为测试隔离 service）。
- **待办唯一验证**：提交线 + 上述 2 个未提交修复之上，完整跑一轮 `Scripts/run_tests.sh`，预期 279/279。跑前确认无 SecurityAgent 弹窗、钥匙串已解锁。
- 实机 `~/Applications/BangWoFenXi.app` 仍是 7/22 旧包（含恢复横幅误显示 bug，源码已修未安装）；`build/BangWoFenXi.app` 为 7/24 新包（含全部修复），**未安装**。

### 真实数据安全（重要）
- 真实录音（52 分钟，paused）自 7/22 19:07 未再写入，其 project 的 runtimeAssetRelativePath 为 null——`ProjectAssetRepair` 已就绪：下次启动新包时自动幂等补写（仅当标准路径文件存在且非空；不动录音文件）。
- **不要手工改真实 projects.json**；不要强退/热替换当前任何 App。

### 截图环境（隐私红线）
- 预览变体 `/tmp/BangWoFenXiPreview.app`（bundle id `com.zhaobo.BangWoFenXi.preview`，独立容器）已播种**全合成**数据（2 项目）；播种器源码 `/tmp/bwfx-preview-seed/main.swift`。
- 截图**只能按窗口区域截取**（System Events 取 window position/size → `screencapture -R x,y,w,h`），**严禁全屏截图**（桌面有微信与 github-recovery-codes 等敏感文件）。待截：首页、首次确认（含隐私文案）、工作台无错误横幅、笔记正文+保存时间、麦克风失败重试（变体无麦克风权限，天然失败态）、1280/1440、极限拖宽（可用 `defaults write com.zhaobo.BangWoFenXi.preview bwfx.workspace.leftFraction -float 0.95` 造极端比例）。

### 交接纪律
- 不 commit（含上述 2 个测试修复，除非老板另行许可）、不 push、不安装；完成后交 Codex 二次审计。
- 本机多用户高负载时异步测试曾抖动，已全部条件轮询化；新增测试请沿用"每用例独立临时目录 + 轮询等待"两条纪律。

---

## 2026-07-24：知识花园版 阶段 C——音视频导入

实施依据：03 号文档 §6.2 / §7.2 / §9.1 / §16 阶段 C。由 Claude（接任维护者）实施。

**核心设计**：提取音轨落到与实时录音相同的标准路径 `Meetings/<id>/recording.caf`——回放、删除、资产修复、转写全部零特殊分支复用；原文件只复制留档（`source-original.<ext>`）绝不修改；失败重试不重新复制原件（03 §6.2）。

**新增**：
- `Core/Import/AudioImportService.swift`：`AudioImportServicing` 协议（§9.1 签名 + onProgress 扩展）＋ AVFoundation 实现。inspect 真实加载音轨/时长（不按扩展名猜测）；提取用 AVAssetReaderAudioMixOutput → 48kHz 单声道 Float32 PCM，临时文件原子落位；明确错误分类（fileNotReadable/noAudioTrack/zeroDuration/undecodable/extractionFailed）。
- `Core/Import/FileTranscriptionRunner.swift`：读 PCM 按块喂 `LocalTranscriptionServicing`（独立实例，不与实时会话争抢），循环读满防短读（血泪教训 #5），TranscriptReconciler 同口径去重，取消响应（withTaskCancellationHandler，流未收尾也不悬挂）。
- `Core/Import/ImportPlanner.swift`：纯逻辑 Job 编排/续跑/状态判定。**纪律：任何失败（含可重试）阻断后续阶段**（测试抓出的真实缺陷：跳过失败继续跑会让转写在无音频时产生次生 failedFinal）；分析 Key 未配置不创建分析 Job，不伪造等待状态；中途配置好则续跑时补建。
- `Features/ProjectImport/ImportProcessingController.swift`：App 级流水线（AppEnvironment 懒持有，离开工作台后台继续；首版一次一个导入）。每阶段状态立即持久化，重启从最后完成阶段续跑；取消回 pending 非失败；**持久化为字段级合并**——流水线只写自己拥有的字段（状态/Job/片段/资产路径/快照/时间），不覆盖工作台并发编辑的笔记/标题（两副本整对象覆盖会互相冲掉写入，测试已锁定该行为）。
- 首页：导入按钮真实化（NSOpenPanel，audio+movie）＋整页拖放；处理中禁用并如实提示"一次一个"；检查失败明确弹窗（不创建项目）。
- 工作台：导入进度条（各阶段图标+百分比+暂停/继续处理按钮）；Job 状态迁移驱动从存储刷新副本（进度百分比不触发重载）；导入流水线活动中不误报"未正常结束"。

**顺手修复阶段 B 遗留缺陷**：只读项目（ready/导入完成）打开工作台时左栏文稿永远为空——`transcription?.segments ?? meeting.segments` 中控制器非 nil 而 segments 为空数组时不会回退；改为空数组时回退 meeting.segments。

**杂项**：Package.swift 排除 Agent.md/开发日志.md（既有欠账：manifest 重规划时报 unhandled 警告，违反 0 警告纪律）。

**测试**：新增 4 套件 30 用例（规划 14、导入服务 8、文件转写 6、流水线 8——含并发拒绝、取消、续跑用副本、字段级合并、部分完成）。夹具全部合成正弦波，无真实录音。真实媒体集成探针：`BWFX_IT_MEDIA=1` 时用系统 afconvert 合成真 AAC m4a 走真实检查+提取全链路（已跑通：48kHz 提取、时长误差 <0.1s）；默认跳过保持套件快速。

**验证状态**：`swift build` 0 警告；`Scripts/run_tests.sh` **313/313 全绿（52 套件）**；含探针 314/314。
**尚未验证（阶段 C 验收余项，待实机）**：mp3/mp4/mov 真实样例（afconvert 无法合成 mp3/视频容器，需真实文件人工验收）；60 分钟真实录音导入全链路；导入项目在实机 UI 的完整操作（拖放、进度、续跑）。

---

## 2026-07-24：知识花园版 阶段 D——通用分析与场景模板

实施依据：03 号文档 §8.4 / §10 / §16 阶段 D。主体代码由上一窗口完成后中断（掉线时遗留 1 处编译错误、测试为零）；本窗口接手复核设计、修复中断现场、补齐全部测试。

**V2 数据模型**（`Models/ConversationAnalysis.swift`）：`AnalysisItemCategory` 单一枚举 20 类覆盖四场景（整理层 6 + 语义层 6 + 场景增强 8，含 knowledgeSeed 供阶段 F 消费）；`AnalysisItem`（epistemicStatus/confidence/evidenceSegmentIds/subjectSpeakerId）；`ConversationAnalysisSnapshot`（version/analyzedThroughMs 游标/headline/detectedScenario）。`Project.analysisSnapshots` 为权威容器，自定义解码兼容阶段 D 之前的旧 JSON（缺键 → 空数组）。

**协议与校验**（`Core/Analysis/ConversationAnalysisSchema.swift`）：snake_case wire 名 ⇄ 枚举双向映射；`ConversationAnalysisSnapshotBuilder` 沿用 V1 红线——证据为空/无法解析/引用不存在片段/混入假证据 → 整条丢弃；枚举白名单外的值不猜测归类；说话人代号回映射本地 ID，未知代号如实 nil。

**语义分析师 Prompt**（`ConversationAnalysisPrompt.swift`，老板 07-24 指示的人设升级）：模型以「资深中文语义分析师」身份进入——穿透口语提炼语义与意图而非复述；显式告知转写可能含同音字/错别字，按上下文还原语义但证据引用不改原文；V1 的 8 条红线全部保留；场景增强规则只改侧重不改红线（客户拜访/课堂/采访/自由记录 + 未指定时要求模型建议场景）。

**传输复用**：`KimiAnalysisService` 抽出 `rawAnalysisText`（V1/V2 共用同一 HTTP/超时/日志/围栏剥离路径）；`KimiConversationAnalysisService` 只换系统指令与 DTO 解码，非法 JSON 抛 invalidResponse（保留上一版）。

**调度**（`ConversationAnalysisController`）：与 V1 同纪律——触发（≥3 片段或 45s + 10s 防抖）/串行单请求（进行中合并到下一轮）/游标（analyzedThroughMs，失败不推进）/失败保留上一版/401、未配置 Key 暂停。新增：场景自动建议（用户未手选时采纳模型 detectedScenario，手选优先绝不覆盖）。

**UI 与接线**：`ConversationAnalysisView` 按类别分组渲染（总结/动机与目的两页签、明确表达/AI 推断一眼可辨、证据回链左栏）；工作台切 V2 控制器（快照直接挂 Project，不再经运行时 Meeting 桥接）；顶部场景选择器（自动建议 + 用户随时修正）；旧谈判项目（仅迁移快照、无 V2 快照）继续走旧渲染兼容回看；导入流水线分析阶段切 V2，字段级合并新增 analysisSnapshots 与 scenario（手选优先）。

**接手修复**：① 掉线遗留编译错误（`reloadImportedProjectFromStore` 对 guard 已解包的 project 二次 `if let`）；② 同函数漏刷 `analysisSnapshots` 字段——导入流水线后台生成的分析刷不进打开中的工作台；③ `ImportProcessingControllerTests` 未随流水线切 V2 协议（真实类型错误），Mock 与断言改 V2 口径（断言强度不变）。

**测试**：新增 3 套件 31 用例 + V2 Mock（Schema/构建 12：证据存在性、白名单、代号映射、四场景同一 Schema、页签两分完备、Project JSON 往返与旧 JSON 兼容；输入组装与指令 7：只发代号不发姓名/标题、previous_state 压缩携带、注入样例包裹于 untrusted 对象且不出现在其外、四场景指令均含完整红线、场景规则引用的类别全部在 Schema 白名单内；调度 12：防抖/游标增量/串行不堆积/非法 JSON 保留上一版/401 暂停恢复/最终分析全量/临时片段排除/场景建议采纳与手选优先/attach 恢复最高版本）。

**验证状态**：`swift build` 0 警告；`Scripts/run_tests.sh` **345/345 全绿（55 套件）**；`git diff --check` 通过。
**尚未验证（阶段 D 验收余项，待实机）**：真实 Kimi 网关联调 V2 指令与 JSON 合同（本地仅 Mock 验证）；四场景真实对话各一次验收；旧谈判项目实机回看；实机包仍未更新（`~/Applications` 为 7/22 旧包）。

---

## 2026-07-24 下午：阶段 D 收尾——真实网关探针、打包安装、存档（致实机验收前）

老板指示「把能做的一次性做完再存档」。本节完成全部无需实机交互的事项。

**一、真实 Kimi 网关探针（阶段 D 验收关键项，✅ 通过）**
- 探针方式：全合成客户拜访对话（10 片段，含 1 条注入攻击样例）走 App **真实代码链路**（`ConversationAnalysisPrompt` → `ConversationAnalysisInputAssembler` → `KimiConversationAnalysisService` → 严格解码 → `ConversationAnalysisSnapshotBuilder` 证据校验）；Key 经环境变量注入独立测试 Keychain 条目（`…tests.stage-d-probe`），用完即删，未进日志。探针源码 `/tmp/bwfx-stage-d-probe/`（临时，不入仓）。
- 结果：**74 秒返回合法 V2 JSON（一次通过，无截断）**；21/21 条目全部通过证据校验；`detected_scenario=clientVisit`（高置信，判定正确）；明确表达 15 / AI 推断 6，事实类全 explicit、动机类全 inference（口径正确）；**注入样例未被服从**——模型把「忽略之前的要求…」当对话数据分析，并主动输出为 contradictionEvasion 条目（低置信推断），符合红线。
- ⚠️ **网关迁移发现（需老板决策）**：探针最初打 App 硬编码的 `agent-gw.kimi.com` 全部 401（新体系 kimi-code OAuth token 不被接受）；改打 `https://api.kimi.com/coding/v1/messages` 则 `x-api-key` 与 `Bearer` 均 200。结论：agent-gw 是旧通道，App 中 7/21 配置的旧 Key 目前可能仍可用但随时可能失效；新体系 token 900 秒过期 + refresh_token 刷新，不适合静态存 Keychain。**未改动 App 代码**（避免使现有可用 Key 失效），已写入 Agent.md §4 待决策。**实机测试若遇分析 401，原因即此。**

**二、含真实媒体探针的全量测试**：`BWFX_IT_MEDIA=1 Scripts/run_tests.sh` 全绿，真实 m4a（afconvert 合成 AAC）检查+提取全链路通过。

**三、打包与安装**
- `Scripts/make_app.sh release` 打出新包并**已安装** `~/Applications/BangWoFenXi.app`（旧 App 确认未在运行后替换；去 quarantine；`codesign --verify` 通过）。新包含：阶段 B 补强（恢复横幅门控、录音资产回写/补修、设置返回路由）+ 阶段 C 导入 + 阶段 D 通用分析全部内容。
- 真实数据只读复核：projects.json 7 项目可解码、迁移标记在、meetings.json 原样；paused 真实录音项目（52 分钟）的 580MB recording.caf 就在标准路径，`ProjectAssetRepair` 三条件全满足——**首次启动新包时将自动幂等补写资产路径**，无需手工干预。

**四、文档存档**：Agent.md 全面刷新（知识花园版架构、阶段进度 A–D、网关迁移风险、血泪教训 8–10 条新增、未决事项重排）；本日志补齐。

**留给实机验收的清单（老板执行）**
1. 启动 `~/Applications/BangWoFenXi.app`：确认 7 个历史项目完好、paused 录音项目资产自动补写后可回放。
2. 新录音走通三栏工作台：转写上屏、V2 分析两页签（总结/动机与目的）、场景标签自动建议与手动修正、笔记自动保存。
3. 导入一个真实 m4a/mp3/mp4：进度条、中途退出续跑、完成后左栏文稿与分析可见。
4. 旧谈判项目打开：迁移快照兼容回看（旧渲染）。
5. 若分析持续 401 → 即网关迁移问题，回报后决策切换 `api.kimi.com` 方案。

---

## 2026-07-24 傍晚：Kimi 网关迁移落地——账号登录（OAuth 设备码 + 自动刷新）

老板决策「换成你现在的 key」。经核查本机 kimi CLI 用的是 kimi-code 新体系 OAuth（access_token 900 秒过期 + refresh_token 轮换），静态存 Keychain 不可行，遂实现账号登录取代旧静态 Key。

**一、网关切换**
- `CloudModelConfig.analysisBaseURL`：`agent-gw.kimi.com/coding/` → `api.kimi.com/coding/`（旧通道对新体系 token 全部 401，实测确认）。

**二、OAuth 设备码登录（`Core/Security/KimiOAuth.swift`）**
- `KimiOAuthClient`：设备授权 + 轮询换 token + 刷新，全部走 `auth.kimi.com`（与 kimi CLI 同一套流程、同一公开 client_id `17e5f671…`，从 CLI 二进制核出）。pending/slow_down/expired/denied 分类，401/invalid_grant → unauthorized，5xx → network。
- `KimiOAuthTokenStore`：凭证整组 JSON 存 Keychain 独立条目 `kimi-oauth`（与静态 `kimi` 条目并存互不影响）；损坏数据如实视为未登录。过期时刻归一化整秒（JSON 秒数往返有浮点误差，否则整组相等断言失败——已记血泪教训 11）。
- `KimiCredentialProvider`（**actor 串行化**）：剩余 <300s 自动刷新，**refresh_token 轮换后立即持久化**（否则旧值被服务端作废，下次刷新被迫重登）；优先级 OAuth > 静态 Key（后备）；刷新被拒 unauthorized（保留凭证）、网络失败 network（可重试不清凭证）、皆无 missingAPIKey。

**三、接线**
- `KimiAnalysisService`：`apiKeyStore` 直读改为经 `KimiCredentialProviding.validCredential()`（V1/V2 共用传输层，两条路径同时受益）。
- `AppEnvironment`：新增 `kimiOAuthTokenStore`；`isAnalysisConfigured` = 账号已登录 或 静态 Key 已存；`refreshCloudConfiguration` 同步。
- 设置页：新增「Kimi 账号（推荐）」区——登录/重新登录/退出、等待授权时展示确认码、已在浏览器打开授权页；静态 Key 区降级标注为「后备」。`KimiLoginController`（@MainActor @Observable）驱动设备码流程、开浏览器、轮询、落库、状态回调。

**四、真实全链路探针（✅ 通过）**
- 消费本机 CLI 当前凭证 → 独立测试 Keychain（**故意置过期强制走刷新**）→ App 真实代码：`KimiCredentialProvider` 真实刷新（auth.kimi.com）→ `testConnection()` 真实 messages 请求（api.kimi.com 新网关）→ **200，5.8s**。轮换出的新 refresh_token 已原子写回 CLI 凭证文件（CLI 不受影响，实测 CLI 凭证 878s 后过期正常）。测试 Keychain 条目已清理，未打印任何 token。探针源码 `/tmp/bwfx-oauth-probe/`（临时，不入仓，已删中间产物）。

**五、测试**：新增 2 套件（Kimi OAuth 11 例：客户端请求形态/轮询分类/刷新分类、凭证存储往返与损坏兜底、凭证提供者刷新/回退/失效/优先级、登录控制器 happy path 与拒绝/取消；OAuth Mock 客户端 + 打开 URL 记录盒）+ `KimiAnalysisAPITests` 补 3 例（OAuth token 优先、临期先刷新、刷新被拒不发请求）+ Key 分家套件补账号登录计入配置态。既有断言随网关地址更新到 `api.kimi.com`。凭证存储全部隔离到测试 service（血泪教训 9）。

**验证状态**：`swift build` 0 警告；`Scripts/run_tests.sh` **360/360 全绿（56 套件）**。
**存档状态**：本地提交 `aac2843`；提交时 GitHub 不可达（443 连接失败，与 07-23 同症状），**待网络恢复后 `git push`**。`build/BangWoFenXi.app` 新包已打好；提交时实机 App 正在运行，按红线未热替换——退出 App 后按 Agent.md §2 安装。
**实机验证（2026-07-24 16:37）**：✅ 老板真人走通设备码登录——App 展示确认码 → Safari 打开授权页（默认浏览器 Chrome 异常，用 `open -a Safari` 指定打开绕过）→ kimi.com 账号授权通过（账号设备列表出现"未知设备"即本 App）→ App 自动转为已登录。
**尚未验证（待实机）**：登录后新录音/导入的真实分析产出（网关本身已由探针验证 200）。旧静态 Key 属遗留通道，已建议在设置页删除。
**遗留（与本 App 无关）**：本机默认浏览器 Chrome 无法正常打开页面，待老板自查。

---

## 2026-07-26：审计修复工单 H1–H4、M1–M5+L1

实施依据：`../04_帮我分析_审计修复工单_20260726.md`。本轮严格停留在工单范围，不启动阶段 E，不安装、不热替换、不 push。

**逐项提交**

1. `eafadf0` H1：OAuth 临期刷新改为共享 in-flight `Task`，12 个并发请求只刷新一次；AppEnvironment 的 V1/V2 分析路径共享同一凭证提供者实例。
2. `b68e354` H2：补 `com.apple.security.files.user-selected.read-write` entitlement；文件面板和拖放导入在检查与原件复制期间覆盖 security-scoped access，续跑只依赖项目目录副本。
3. `0b9d23d` H3：建立 Project 字段所有权表与统一合并入口；笔记、标题、场景、人工片段和流水线字段不再由旧副本整对象互相覆盖，Mirror 测试锁定 24 个字段全部登记。
4. `6a21330` H4：音频连续 5 次写失败仅告警一次，录音服务自动暂停并展示如实横幅；偶发失败只记脱敏日志。
5. `8526693` M1：实时最终片段以 2 秒有界窗口批量落盘；暂停、结束、状态同步和视图消失立即 flush，连续 20 片段合并为一次写入。
6. `747c167` M2：V2 分析 pendingFire 补发重新通过 trigger/debounce；可控挂起请求与假时钟测试锁定。
7. `d566ec0` M3：V2 分析快照纯函数修剪为最近 5 版，版本号继续单调递增；legacySnapshots 不动。
8. `9c82e37` M4：projects.json 解码失败时原字节改名为 `projects.corrupt-<ISO8601>.json`；备份未完成时拒绝写入，备份完成后可安全重建；首页显示真实备份文案。
9. `2d48ad6` M5+L1：poll/refresh 遇非 JSON 非 2xx 响应按可重试 network 分类（401/403 仍 unauthorized）；损坏 OAuth 凭证记录 `kimi_oauth_credentials_corrupted`。
10. `a1f7075`：把 OAuth 默认时钟改为等价 Sendable 闭包，清除独立 soak 编译器发现的 2 处并发警告。

**验证结果**

- 每项完成后均执行 `swift build`（0 警告）和 `Scripts/run_tests.sh`；用例数依次为 362、362、366、369、371、372、375、378、380，全部通过。
- 最终代码状态执行 `BWFX_IT_MEDIA=1 Scripts/run_tests.sh`：**380/380，56 套件全绿**；真实 `afconvert` 合成 AAC 的检查与音轨提取链路通过。
- 最终代码状态执行 `Scripts/soak_test.sh`：**SOAK PASS**；180 秒音频、4× 速率、7,938,048 帧、9 个分片、3 段暂停累计 9 秒，RSS 13.6→14.5MB（+0.8MB），编译 0 警告。
- `Scripts/make_app.sh` 已生成 `build/BangWoFenXi.app`；`codesign --verify --deep --strict` 通过，arm64/ad-hoc；Sandbox、麦克风、出站网络和用户选择文件读写 entitlement 均存在。

**状态边界**

- 本地已做：工单 H1–H4、M1–M5+L1 全部完成并独立提交。
- 本地已验证：构建、380 项自动化、真实合成媒体探针、缩短耐久、打包签名与 entitlement 检查通过。
- 远端已生效：否；本轮未 push，`origin/main` 仍停在审计前。
- 客户可交付：否；仅 ad-hoc 内部包，未公证，且实机验收欠账未清。
- 真实外发或真实使用已验证：否；本轮包未安装、未打开、未外发。

**待实机验收**

1. 真实 mp3 与 mp4 分别走首页文件面板、拖放两条路径，确认检查、原件复制、提取、转写和重启续跑。
2. 在可恢复的测试磁盘/目录制造磁盘满或录音文件不可写，确认连续失败后自动暂停、暂停区间正确且红色横幅文案如实。
3. 安装本轮包后跑一次录音→转写→分析→结束 golden path，并检查项目库损坏时的首页备份提示。
4. `Scripts/soak_test.sh 3600 1` 完整 60 分钟档及长文件导入仍未执行。

---

## 2026-07-26 深夜：二次审计通过、推送与实机安装（Claude 复核存档）

上一节"状态边界"中「远端已生效：否 / 本轮包未安装」的记录至此被本节取代。

**一、二次审计（Claude 逐提交复核，✅ 全部通过，无返工项）**

- 独立复核（不依赖执行方报告）：本机现场跑 `Scripts/run_tests.sh` **380/380 全绿**；`swift build` 0 警告；对 `build/BangWoFenXi.app` 实际打包产物核验 entitlements（sandbox / 麦克风 / 网络 / user-selected 文件读写四项俱在）。
- 逐提交代码审读结论：
  - H1 单飞实现正确（actor 内共享 refreshTask + 代际防过期清除；"先持久化再返回"顺序保持；AppEnvironment 唯一凭证提供者 + 唯一 transport，默认参数绕开共享的风险已处理）。
  - H2 security-scoped 作用域覆盖 inspect 与提取、复制原件后即释放，续跑只读项目目录副本，符合设计。
  - H3 实现质量超出工单：24 字段所有权表、片段级双向合并（流水线不冲人工修订/星标，工作台只带人工变更）、Mirror 守护测试堵死第三次漏字段。
  - H4 实时线程内仅锁内计数，日志与回调分别派发 utility 队列/主线程，实时安全。
  - M4 备份用 moveItem 保留原始字节且带写保护标志，优于工单要求。
  - 顺带提交 `a1f7075`（OAuth 时钟并发警告）改动 2 行等价替换，认可不算越界。
- 残留观察（不阻塞，记录在案）：
  1. M4 备份触发后用户会先看到空库+提示文案，人工恢复靠备份文件——实机验收时值得演练一次恢复流程。
  2. `ProjectRuntimePersistenceController` 防抖窗口从第一条未保存片段起算（有界 2 秒，非滚动重置），是优于工单的选择，注释已说明。

**二、推送（✅ 远端已生效）**

- `git push origin main`：`ae6d619..98faf67`，11 个提交全部推送；`git ls-remote` 核验远端 `refs/heads/main` = `98faf67` 与本地 HEAD 一致。

**三、实机安装（✅ 已安装并启动）**

- 安装前确认旧 App 未在运行（pgrep 无进程，不打断任何录音）。
- `build/BangWoFenXi.app`（07-26 23:26 打包，含全部 11 项修复）签名校验通过 → 复制 `~/Applications/` → 去 quarantine → 安装后二次 `codesign --verify --deep --strict` 通过，user-selected entitlement 在包内确认存在。
- App 已启动交老板实机测试。

**四、交接给下一窗口的当前状态**

- 本地已做/已验证：审计修复全部落库；380 测试、打包、签名均现场复核通过。
- 远端已生效：是（`origin/main` = `98faf67`）。
- 真实使用验证中：老板正在实机测试，测试清单（按优先级）：
  1. 启动检查：历史项目完好；若首页出现"数据文件损坏"提示（不应出现）立即停手回报。
  2. 导入（验证 H2，最关键）：真实 mp3/mp4 各走「文件面板」与「拖放」两条路径；中途退出重开验证续跑。
  3. 录音 golden path + 导入处理中编辑笔记/标题不丢（验证 H3）。
  4. 长录音（10 分钟+）观察分析请求有间歇不连发（验证 M2）。
  5. 磁盘满自动暂停（H4）可跳过，或在快满的外置盘上试。
- 待实机验收欠账不变：60 分钟完整 soak、长文件导入全链路、M4 恢复流程演练。
- 客户可交付：否（ad-hoc 内部包，未公证）。

## 2026-07-26 深夜：界面美化、App 图标与声纹/实时分析强化（Claude 执行）

老板压缩上下文后的新任务：1）界面太丑，参考得到大脑美化；2）设计并安装 App Logo；3）功能强化——声纹识别落地、实时分析优化。本轮由 Claude 直接开发（非工单模式），4 个提交，388/388 测试通过（新增 8 项），已打包安装。

### 一、设计系统与全界面美化（a318af7）
- 新增 `App/Theme.swift`：BWTheme 配色（暖橙主色 #FA7320 系渐变、纸感底色、明暗双态动态色）、`bwCard()` 卡片修饰、`BWBadge` 徽标、`BWSpeakerDot` 说话人圆点、`BWBrandMark` 品牌标。
- 首页：品牌头部（logo + 副标题）、渐变大卡「开始录音」/白卡「导入音视频」双入口、项目行卡片化、拖放悬停高亮描边。
- 工作台：三栏用纸感/栏底色区分层次，栏头加橙色强调竖条，顶栏/底部录音条换 .bar 材质。
- 转写行重塑：说话人圆点 + 两行结构（元信息行/正文行）；常态「已确认」不再显示状态标签，只有识别中/人工修订/待重试才显示（降噪）。
- 分析卡片：headline 橙色高亮卡（sparkles 图标）、类别竖条标题、明确表达/AI 推断换 BWBadge（绿/橙，红线的一眼区分保持）。

### 二、App 图标（c9e1c97）
- `Scripts/make_icon.swift`：CoreGraphics 程序化绘制——暖橙渐变圆角方 + 白色 5 竖条声波 + 顶部分析弧线与端点（「录音 → 洞察」），与 BWBrandMark 同一设计语言；生成全尺寸 iconset。
- `iconutil` 打包为 `Resources/AppIcon.icns`；Info.plist 增加 CFBundleIconFile；make_app.sh 打包时拷贝。改图标只需改脚本重跑。

### 三、声纹识别落地（2efebb4）
- 现状：云端分人链路（分片上传 + KnownSpeakerReference 样本匹配）早已打通，但 V2 工作台没有任何录入说话人/声纹样本的入口——链路空转，这是「识别不了声纹」的真正原因。
- `Speaker` 增加 `voiceSamplePath`/`voiceSampleDurationMs`（V2 字段，合成 Codable 对旧数据容错）。
- 新增工作台「说话人」按钮 → `SpeakerPanelView`：添加/编辑说话人（姓名角色只存本地，云端只见 p_01… 代号）、录制/试听/重录 2–10 秒声纹样本（复用 V1 VoiceSampleController 与校验器）；录音进行中麦克风被占用时置灰并如实说明。
- 运行时桥 V2 样本优先、legacy 兜底；`DiarizationController.refreshKnownSpeakers()` 编辑后刷新映射（已确认片段不回改）；持久化走 `.speakers` 所有权通道。
- 注意：分人 provider 仍未配置（`diarization` Key 空 → 灰态零请求）。声纹样本录好后，需在设置页配好分人 Key 才真正发请求。provider 决策仍开放。

### 四、实时分析优化（4837493）
- `AnalysisTrigger.liveRecording` 预设：2 新片段 / 5 秒防抖 / 30 秒闲置上限（原 3/10/45），失败退避保持 30 秒防热循环；默认值与导入流水线不变，既有测试口径不变。
- 提示词 persona 增加「增量维护」段：previous_state 的保留/更新/删除语义显式化，headline 必须反映全局，立场变化用 stance_change 显式呈现不静默改写；加深度要求（优先输出读稿人自己看不出的东西，一条高质量判断胜过五条复述）。红线 rules 原文一字未动，注入防护口径不变。
- 中栏头新增分析活动指示：分析中转圈、最近失败黄点（hover 看原因）。

### 验证与安装
- `swift build` 0 警告；`Scripts/run_tests.sh` 388/388（56→58 套件，新增说话人面板逻辑 5 项 + 实时触发预设 3 项）。
- 打包含图标，签名校验通过；确认数据文件 8 项目全部 ready（无录音进行中）后 osascript 正常退出旧实例，安装并启动新包。

### 给老板的实机验收点
1. Dock/访达图标是否显示新 logo（首次可能需要 `killall Dock` 或注销刷新图标缓存）。
2. 首页/工作台/转写/分析卡片新观感是否符合预期（明暗两种外观都看一眼）。
3. 工作台顶栏「说话人」→ 添加说话人 → 录 2–10 秒样本 → 试听；录音进行中入口应置灰。
4. 录一段 10 分钟对话：分析更新节奏应明显变快（首版约 45–50 秒内出现，此后新内容安静 5 秒即更新），中栏头能看到「分析中」转圈。
5. 声纹识别端到端要生效还需配置分人 Key（设置页），否则说话人仍显示「待识别」，属预期。

### 残留
- 上一轮欠账不变：60 分钟 soak、长文件导入全链、M4 损坏恢复演练。
- 设置页未做视觉重塑（Form 原生样式尚可，优先级让位）；得到大脑式「知识花园」深色阅读模式可作为下一轮候选。

## 2026-07-27 凌晨：全局专业词库 + 转写纠错（Claude 执行）

老板新需求：1）预先导入词库（`词库/存档_20260727_定稿/`，148 词定稿），让 AI 优先认识专有名词少听错；2）转写听错时点选错词一键全局纠错。1 个提交（70d6cd3），402/402 测试（新增 14 项），已打包安装。

### 词库
- `Core/Lexicon/LexiconStore`：App 级 `<base>/lexicon.json`（原子写；损坏/缺失返回空不阻塞）。解析兼容每行一词与顿号/逗号分隔、`#` 注释、40 字上限、去重保序；合并追加不打乱既有顺序。
- 设置页新增「专业词库」区（第一屏）：从文件导入（NSOpenPanel + 安全作用域）、粘贴导入、清空、纠错规则查看/删除。
- 词条流向三处：① 实时转写 `contextualStrings`（words 只改善识别不改写原意，App 既有约定）；② 导入流水线转写上下文（原来传空数组）；③ 分析系统指令新增「已知名词表」段（≤200 词，指导模型把同音误写按名词表还原；红线 rules 原文未动，注入防护测试通过）。
- **词库文件不由代码直接读**：红线禁止手工改 Application Support，导入动作由老板在设置页完成（一次导入永久生效，跨项目共享）。

### 纠错
- `TranscriptCorrector` 纯函数引擎：`applyGlobal` 整场替换（命中片段标 `.manual/.edited`，享受既有防云端覆盖保护；临时片段跳过避免视觉跳动）；`autoCorrect` 规则套用；`isValidRule` 拒绝空/相同/正词含错词（防替换膨胀）；`mergeRule` 同错词后写覆盖。
- 交互：转写行右键 →「纠错（全局替换）…」→ 弹层显示原文（可选中拷贝）→ 填错词/正词 → 实时预览命中片段数 → 一键全局纠错。
- 纠错生效范围：① 当场整场替换并持久化；② 规则写入 lexicon.json，**之后所有转写自动套用**——本地最终结果与云端确认结果在合并点统一过 `autoCorrect`（两侧文字一致，不影响重复判定），导入转写完成后批量套用；③ 正词自动加入词库，下次优先识别。
- 设置页可管理规则（查看/删除），删除后不再自动套用（已改的历史文字不回滚）。

### 验证与安装
- swift build 0 警告；402/402（61 套件，新增词库 4 + 纠错 7 + 名词表 3）。
- 已打包安装启动。上一轮的 5 个提交因 github.com 连不上仍未推送，本轮一并待推。

### 给老板的操作指引
1. **导入词库（一次性）**：设置 → 专业词库 → 从文件导入 → 选 `词库/存档_20260727_定稿/词库_通用_导入版.txt`（148 词全量）。开专场会前也可再导入 `按场景/*.txt`（追加合并，去重）。
2. **纠错**：工作台左栏转写行右键 →「纠错（全局替换）…」；纠一次，整场改 + 以后自动改 + 正词进词库。
3. 验证点：导入后录一段带「经销商/动销/窜货」等词的话，观察识别正确率；故意说个词库外的同音词 → 右键纠错 → 确认整场替换且后续转写自动正确。

## 2026-07-27 凌晨：导入转写悬挂修复（血泪教训 #13）+ 存档

老板实测报「导入音频无法转写」。已定位修复（236904f），402/402 通过，修复包已安装。

### 现象与定位
- 导入 m4a 通话录音（113 秒）：音轨提取完成，转写 job 卡在 running 0%，无错误、无日志、App 0% CPU；线程采样只剩空闲工作队列。
- 数据侧：recording.caf 完整（Float32 48kHz，5,440,363 帧），源文件正常。
- **独立探针**（/tmp/speech_probe.swift 直接用 SpeechAnalyzer 转写同一个 recording.caf）：1 秒内 8 段全部产出。系统层完全正常 → 纯 App 层缺陷。

### 根因（Core/Transcription/LocalTranscriptionService.swift）
`AppleSpeechTranscriptionService.results` 是 **init 时创建的单一 AsyncStream，cleanup 从不 finish**：
- 实时链路无感：LocalTranscriptionController 的收集循环被显式 cancel。
- 导入链路致命：FileTranscriptionRunner 注释写着「喂完 finishSession 后流自然结束」，喂完全部帧后 `await collectTask.value` 等一个永远不会终结的流 → 永久悬挂。挂起点在收尾等待，所以进度停在开头、结果一段都收不到。
- 为什么测试没拦住：Runner 测试用 MockLocalTranscriptionService（会 finish 自己的流），380 项真实媒体测试覆盖到提取为止；真实 SpeechAnalyzer 整链只有实机才走到。

### 修复
- 结果流**每会话新建**（`results` 改计算属性，加锁返回当前会话流）；收集任务持有本会话 continuation（不经 self 动态查找，消除跨会话串流可能）；**cleanup 显式 finish**，消费方随之退出。
- Agent.md 血泪教训 #13：「等流结束的一方永远要问一句：谁负责终结这个流？」

### 验证与交付
- swift build 0 警告；402/402（61 套件）。修复包已打包安装启动。
- 卡住的项目（E3D9…，通话 13561154333）数据完好：提取完成、0 片段；打开项目点「继续处理」即从转写阶段续跑。

### 交接状态
- 本轮全部提交已推送（含界面美化 5 提交、词库纠错 2 提交、本修复 1 提交）。
- 待老板实测：① 重开卡住的导入项目点「继续处理」，确认转写产出；② 再导一个新 mp3/mp4 走全链；③ 词库导入（设置 → 专业词库 → 词库_通用_导入版.txt）后验证识别改善；④ 右键纠错闭环。
- 既有欠账不变：60 分钟 soak、M4 损坏恢复演练、分人 provider 决策。

## 2026-07-27 凌晨：本窗口收尾存档（Claude 交接）

本窗口（2026-07-26 深夜 ~ 07-27 凌晨）共 9 个提交，全部已推送，工作区干净，远端一致（HEAD = 286a287）。实机安装包为含全部改动的最新构建（App 运行中）。

### 本窗口交付一览
1. **界面美化**（a318af7）：设计系统 BWTheme + 首页/工作台/转写/分析卡片全面重塑。
2. **App 图标**（c9e1c97）：程序化绘制脚本 + icns 接入打包。
3. **声纹面板**（2efebb4）：工作台「说话人」入口，录入说话人 + 2–10 秒声纹样本。
4. **实时分析优化**（4837493）：liveRecording 触发预设（2 片段/5s 防抖/30s 闲置）+ 增量维护提示词 + 分析中指示。
5. **全局词库 + 转写纠错**（70d6cd3）：设置页词库导入（148 词定稿在 `词库/存档_20260727_定稿/`）；右键纠错全局替换 + 规则自动套用。
6. **导入转写悬挂修复**（236904f）：结果流每会话新建 + cleanup 显式 finish；血泪教训 #13。

### 下一窗口接手要点
- 测试基线：402/402（61 套件），swift build 0 警告。
- 待老板实测：卡住项目「继续处理」续跑验证、新文件导入全链、词库导入后识别改善、纠错闭环、声纹样本录制、Dock 新图标。
- 声纹端到端生效前置：设置页配置分人 Key（provider 决策仍开放）。
- 既有欠账：60 分钟 soak、M4 损坏恢复演练、设置页视觉重塑（低优先）。

## 2026-07-28 下午：设置中心、完整总结与受限 AI 编排存档

老板确认实施设置中心、完整总结和 AI Agent 改造。本轮功能提交为 `4a6ae26`，发布分支为 `agent/settings-final-report-ai`。

### 设置中心与运行时配置
- 设置改为 Root 级弹层和五类 `NavigationSplitView`：大模型与 AI、MCP 与知识源、词库与纠错、录音与说话人、存储与隐私。首页齿轮与项目侧栏底部共用同一入口，打开设置不销毁工作台控制器。
- 保留 Kimi，并新增一个 OpenAI-compatible 分析模型。Base URL、Model ID 与独立 Keychain 凭证由 `AIProviderRegistry` 在每次请求开始时读取快照；修改设置只影响下一次请求。
- OpenAI-compatible 仅允许 HTTPS 或 localhost，使用标准 Chat Completions、严格 JSON 解码、禁止重定向；连接测试只发送最小探测内容。
- MCP 从单连接升级为多只读连接：稳定 ID、独立 Token、启停、测试、只读工具筛选和删除均已落地；旧单 MCP 自动迁移。开花按连接 ID 分组和去重，外部 MCP 只接收最长 24 字检索词。
- 词库补齐搜索、单条增删改、批量导入、导出和清空；纠错规则可编辑和删除。界面明确提示当前录音后续片段、下一次录音/导入和历史文稿各自的生效边界。

### 完整总结与 AI 职责
- AI 工作区调整为「实时总结 / 完整总结 / 动机与目的 / 开花」四页签。
- 新增 `FinalReportSnapshot`，最近三版随 Project 持久化；输入指纹覆盖场景、说话人和最终/人工修订片段，文稿变化后旧报告保留并标记需更新。
- 完整总结基于全量分析证据账本生成，条目必须通过真实片段 ID 校验；不读取用户笔记，不调用互联网、Obsidian 或 MCP。
- 录音安全收尾后由 App 级协调器后台生成；导入流水线新增排在 analysis 后的 finalReport Job。失败保留上一版并提供重试，不影响文稿和笔记。
- 最新报告原子写入 `Meetings/<Project UUID>/完整总结.md`；发现外部编辑时先生成时间戳冲突副本。
- AI 职责固定为实时 `ConversationAnalysisController`、无外部工具的 `FinalReportAgent`、只读检索的 `KnowledgeBloomAgent`，以及只做 Provider 选择和上下文组装的 `ProjectAIOrchestrator`。`PromptRegistry` 统一安全红线、场景规则、JSON 合同和 Prompt Version。

### 本地验证
- `Scripts/run_tests.sh`：449/449，通过 66 套件。
- `Scripts/make_app.sh`：成功生成 `build/BangWoFenXi.app`；`codesign --verify --deep --strict` 通过。
- 原生界面：当前设备可提供的 1189×768 和 960×640 内容最小尺寸下，五类设置、四个 AI 页签、项目抽屉、笔记 Inspector 和分隔线辅助调整均可达；设置关闭后原页签状态保留。
- 1440×900 由布局单测覆盖，受当前屏幕尺寸限制未做同尺寸原生截图；深色模式、真实 Kimi/OpenAI-compatible、真实 MCP、真实短录音和真实导入仍待现场验收。

### 状态边界
- 本地已做：代码、测试、产品文档和维护锚点均已更新。
- 本地已验证：449 项测试全绿，App 构建签名和基础原生 UI 验收完成。
- 覆盖安装：本轮未执行，`~/Applications/BangWoFenXi.app` 仍不能视为本提交版本。
- 真实使用：外部模型、MCP、录音与导入闭环尚未验证，未使用客户录音做探针。

## 2026-07-29 凌晨：AI 共创笔记、总结/开花强化与稳定凭证存档

老板连续实测发现：录音时笔记无法稳定输入，AI 对话区过小且不能引用文档；用户在对话中明确纠正转写后，左侧逐字稿没有同步修正；完整总结没有纳入用户主动补充的想法；开花只罗列来源，没有形成一眼能懂的知识速览；弱音频与录音尾句仍会放大识别错误。随后又确认每次 AI 请求都会触发系统钥匙串密码框。本轮在 `agent/settings-final-report-ai` 上统一收口。

### AI 共创笔记与逐字稿纠正

- 将原“我的笔记”升级为项目级“AI 共创笔记”：同一栏内记录想法、发送给 AI、查看反馈并持续追问；草稿按项目自动保存，重开恢复。
- 使用稳定的原生文本编辑器，中文输入法存在 marked text 时不接受 SwiftUI 高频刷新回写，解决录音过程中光标跳动、文字被覆盖和无法连续输入。
- 输入区支持通过“+”引用最多四份有界文档；文本、Markdown、CSV、JSON、PDF、RTF、DOC/DOCX 在本地提取后作为不可信数据发送，文件大小、总字符数和历史携带量均有限制。
- 用户可按项目授权 AI 读取旧笔记；不逐键上传，只在项目对话、开花或完整总结请求开始时读取最新文本。外部 MCP 仍只接收最长 24 字短检索词，不接收笔记或完整逐字稿。
- AI 只有在用户本轮明确给出错词和正词、返回真实片段 ID，且 App 再次核验原文确实命中时，才同步修改左侧逐字稿并写入后续纠错规则。普通讨论、背景补充和模型猜测不得改写录音证据。

### 完整总结、开花与 Kimi K3

- 完整总结改为通读全部最终/人工修订片段，分析快照只作辅助；即使全量分析刷新失败，只要有完整逐字稿仍可继续生成。
- 报告新增带时间戳的“章节概要”。用户主动发送的想法、AI 反馈和获授权的旧笔记单独整理为“我的思考与 AI 共创”，明确不等同于录音事实；这些内容变化会使旧报告显示需要更新。
- 开花可自动处理新出现的高优先知识种子；Obsidian、互联网和只读 MCP 返回后，再由当前分析模型生成“来源速览、关键知识点、与本场讨论的关系”，并保留真实来源 ID。模型不可用时保留检索结果并显示可重试状态。
- Kimi 默认模型调整为 `k3-256k`，可选 `k3` 和兼容模型；K3 保持 thinking，预算提高到 32768。权限不足时如实提示凭证或模型权限，不静默降级。

### 录音、导入与恢复

- 录音条增加实时输入电平和持续低音量提醒；结束录音时先结束 SpeechAnalyzer 输入并等待结果流收尾，再清理采集任务，避免最后一句被提前取消。
- 导入转写补齐取消处理，取消时同步结束结果收集任务；有限文件继续循环处理 AVAudioFile 短读。
- 录音结束时立即记录 `endedAt`；已完成项目的恢复操作改为幂等，草稿或就绪项目不再显示含内部错误类型的异常恢复提示。
- Obsidian 目标已存在时增加目录清单和逐文件内容比对，只有来源与目标完全一致才视为迁移已经完成，避免误接受同名但内容不同的目录。

### 钥匙串根因与规则修正

- 根因是三项叠加：凭证只在 Keychain、ad-hoc 重签名持续改变 App designated requirement、项目规则又把“每次读取配置快照”扩大成每次请求重新解密凭证。此前只禁止交互弹窗，没有建立稳定身份，也没有覆盖旧安装包，因此没有真正解决。
- 新增进程内凭证缓存：首次成功读取后复用；设置中的保存和删除同步更新或清除缓存。自动路径统一使用禁止交互的 `LAContext`，无法静默读取时立即提示重新连接，不阻塞录音和本地转写。
- 生产凭证迁移到 `com.zhaobo.BangWoFenXi.credentials.v2`。旧 ad-hoc 条目原样保留但不自动解密，升级后需在设置中重新连接一次；不读取、不导出、不记录旧密钥。
- 本机登录 Keychain 建立 `BangWoFenXi Local Code Signing` 稳定签名身份。打包脚本默认自动使用该身份；找不到时直接失败，只有明确设置 `BWFX_ALLOW_ADHOC=1` 才允许临时测试，且禁止覆盖用户安装包。
- 项目维护规则同步改为：每次请求只读非敏感配置快照，凭证不得重复解密；安装包必须保持稳定签名。没有把 Keychain 放宽给所有 App，只允许同一稳定身份的《帮我分析》后续版本持续访问。

### 本地验证与状态边界

- `Scripts/run_tests.sh`：482/482，通过 69 套件；新增覆盖项目对话持久化/隐私/引用文档/逐字稿纠正、中文输入法同步、低音量检测、完整总结共创区、K3 合同、Keychain 禁止交互和进程缓存。
- `swift build` 通过；`Scripts/make_app.sh` 生成稳定签名 App，`codesign --verify --deep --strict` 通过。
- 使用两份内容不同、同路径覆盖的稳定签名探针验证：版本 A 保存测试凭证，版本 B 可无提示读取、更新、再次读取并删除；探针只使用假凭证，结束后已清理。
- 新包已覆盖安装到 `/Users/zhaobo/Applications/BangWoFenXi.app`，二进制哈希与构建包一致。旧包保存在 `/Users/zhaobo/Applications/BangWoFenXi.backup-keychain-fix-20260729-011407.app`。
- 原生界面已现场验证：稳定签名版本启动、进入旧项目、打开 AI 设置均未出现 SecurityAgent 密码框；设置页如实显示 Kimi 未配置。真实 K3 请求需老板在设置中重新登录或保存 Key 一次后再验。
- 本轮只做本地存档，未 push；远端仍停留在此前提交。`output/` 下真实录音和转写输出已加入忽略规则，文件原样保留，不进入 Git。

## 2026-07-29 中午：代码审计修复批次 1–5 存档（Bug 1–6 + 界面 1–7）

老板提交三张实机截图并要求全面审计 `帮我分析-同声翻译` 目录。审计产出 `../06_帮我分析_代码审计修复计划_20260729.md`（7 个 Bug + 7 项界面问题 + 6 批执行顺序），老板要求先存档、压缩上下文后再动手。随后按批次 1→5 连续实施。本轮**只在工作区，未提交、未打包、未覆盖安装**。

### 批次 1：异常恢复与时长归零

- **Bug 1（V2 项目异常恢复失效）**：启动恢复此前只读 V1 `meetings.json`，V2 `projects.json` 的 `.recording/.paused/.processing` 残留永远进不了恢复弹窗，用户只能在首页看到一堆红色「录音中」。现在启动时同时读两个存储，id 冲突以 V2 为准；弹窗数据源抽象成 `AbnormalRecoveryItem`（带 `source: .meeting/.project`），弹窗本身不再关心是哪一代模型。
- 「查看」此前会顺手把项目标记成已完成，和「标记为已结束」没有行为差别。现在「查看」只导航不改状态，两个按钮语义分开。
- **Bug 2（`durationMs` 归零）**：崩溃恢复的项目时长恒为 0。新增单一共享口径 `ProjectRuntimeSession.wallClockDurationMs(...)`，`applyRuntime` 与恢复路径都走它——`durationMs` 是含暂停的墙上时钟时间线，不是媒体文件长度，两者不得混用。状态映射同样只保留 `MeetingToProjectMigrator.projectStatus(for:)` 一处，没有引入第三套映射。
- 验证：493/493。

### 批次 2：并发与生命周期正确性

- **Bug 3（单例 `onBuffer` 被抢占）**：`audioCapture` 是单例，`onBuffer` 是可读写属性，旧会话的闭包会在新会话开始后继续收音频，旧会话的清理又会把新会话的 handler 一起清掉。改为**归属令牌**：`setBufferHandler(token:_:)` / `clearBufferHandler(token:)`，令牌不匹配时 clear 直接 no-op。`ProjectWorkspaceView` 与 `LiveMeetingView` 各自持 `audioSessionToken`；`LiveMeetingView` 此前**完全没有 `onDisappear`**，本轮补上。
- **Bug 4（落盘竞态 + 失败即丢）**：`ProjectRuntimePersistenceController` 重复 `flush()` 会写多次盘，写失败则静默丢数据。新增 `flushGeneration` 世代号：只有世代匹配的任务有写权限，重复 flush 只落一次；写失败按 1s/2s/4s 退避自动重试至多 3 次，仍失败则保留 `saveError` 与 `hasPendingChanges`，不假装成功。
- 验证：501/501。

### 批次 3：首页状态语义（界面 1、2）

- 红色「录音中」现在只留给本次运行真正在录的项目：`AppEnvironment` 新增 `liveRecordingProjectIDs`，工作台开录时 `markProjectLive`、结束或离开时 `clearProjectLive`。崩溃残留显示灰色「未正常结束」——红色在 macOS 语境等于「正在进行」，用它表示「上次崩了」是误导。
- 黄色横幅此前写着「打开后可查看或标记结束」却不给按钮。现在有「查看第一个」和「全部标记结束」两个真实入口，后者复用 `markResolvedAfterAbnormalExit`，与恢复弹窗同一口径。
- 验证：507/507。

### 批次 4：词库与拖放（Bug 5、6）

- **Bug 5（改词丢词）**：`updateLexiconTerm` 旧实现走 `LexiconStore.merge`，改成一个已存在的词时数组会静默少一条——用户以为改名，实际删了一个词。现在明确抛 `LexiconEditError.duplicateTerm / .emptyTerm / .termNotFound`，改成自己时静默成功且不写盘；失败时设置页保留编辑中的输入，用户不用重打一遍。
- **Bug 6（拖放不校验）**：`.onDrop` 挂在整页但高亮只画在导入卡片，拖到页面任何位置卡片都会亮；`handleDrop` 又无条件 `return true`，拖文件夹也先"接受"再弹错。现在落点收敛到导入卡片本身（高亮区 = 可放区），`ProjectHomeSupport.acceptsDrop(registeredContentTypes:)` 在返回前同步判定，不支持的类型直接 `false`，系统显示「不接受」光标；落地后再用 `acceptsDroppedFile(at:)` 兜一层。导入进行中不接受新拖放。
- 验证：511/511。

### 批次 5：界面 3–7

- **界面 3**：录音场景选择器（5 个胶囊 + 两行说明占满一张卡）折叠成一行 `录音场景 自动判断 ⌄`，点击展开。它是可选项，纵向空间还给项目列表——此前首屏只能看到 3.5 个项目。
- **界面 4**：项目行抽成独立 `ProjectHomeRow` 以持有 hover 状态（整行可点却零反馈，用户不知道能点）；右侧三行重新分层——时长提为主信息（callout + medium），来源降为小图标带 tooltip，时间最弱，末尾加 chevron。
- **界面 5**：设置页「生效范围」从 `LabeledContent` 改为上下两行 + `fixedSize(vertical:)`，窄窗口换行而不是贴右边截断。
- **界面 6**：「开花」是自造术语，设置页出现 6 次却从未解释。在首次遭遇处补注——页签 tooltip、空态说明、设置页「生效范围」都写明「把选中的内容展开成概念解释与延伸知识」。
- **界面 7**：录音初期三栏全空，信息密度接近零，而用户此刻最需要「在录、收到声音了」的确认，电平条却在窗口底部边缘。`TranscriptPanelView` 新增可选 `liveAudioLevel`，录音中空态直接显示电平条 + 「已收到声音」/「正在收音，暂未检测到声音」。
- 验证：511/511（界面改动本身无法自动验证，见下）。

### 本地验证与状态边界

- `Scripts/run_tests.sh`：**511/511，71 套件全绿**（基线 482 → 493 → 501 → 507 → 511）。`swift build` 0 错误 0 警告。
- 新增测试文件 `BangWoFenXiTests/AudioBufferHandlerOwnershipTests.swift`（含一条打真实 `AVAudioCaptureService` 的用例）；`MeetingRecoveryTests` 增 `ProjectRecoveryTests` 套件 7 条；`ProjectRuntimeSessionTests` 增时长 4 条 + flush 竞态/重试 4 条；`ProjectHomeSupportTests` 增状态显示 6 条 + 拖放校验 3 条；`LexiconAndCorrectionTests` 增改词冲突 1 条。
- **四项界面改动无法自动验证，需老板目视确认**：场景选择器折叠动效、项目行 hover 反馈、窄窗口下「生效范围」换行、真实录音时空态电平条。已在计划状态表如实标注，未用「应该没问题」代替证据。
- **未做**：未 commit、未 push、未 `make_app.sh`、未覆盖 `C:\Users\Ryan\Applications\BangWoFenXi.app`。工作区 19 个文件已改 + 1 个新增文件未跟踪，分支仍为 `agent/settings-final-report-ai`，HEAD 仍是 `11fdb9a`。
- **批次 6（Bug 7：V2 项目删除/重命名）已停在授权边界**：`ProjectStoring` 只有 load/save 无删除路径，首页也没有右键菜单，项目只能累积、磁盘录音无法清理。但删除不可逆且会真删本机项目记录与录音文件，属计划第一节明令需单独确认的动作，已向老板提出三个选项（只做重命名+在 Finder 中显示 / 完整含删除 / 挂起），等决定。

### 环境记录（重要）

- 本机 `swift test` **静默退出 0、一个测试都不跑**（`Agent.md:13` 已记载）。本轮发现 `06_代码审计修复计划` 第六节仍写 `swift test`，已改为 `Scripts/run_tests.sh` 并加注说明。任何"测试通过"的结论必须来自该脚本。

### 批次 6（部分）：项目重命名与在 Finder 中显示（Bug 7 最小版）

- 首页项目行此前无任何右键菜单，项目只能开、不能改名，也没法定位到本机文件。现在 `ProjectHomeRow` 挂 `contextMenu`：**打开 / 重命名… / 在 Finder 中显示**。
- 重命名走 `environment.persist(project, fields: .title)`，只写 `title` + `lastActivityAt`。这一点是刻意的：首页拿到的是列表副本，若用 `.all` 会把工作台或导入流水线此刻正在写的片段、状态、分析快照一起冲掉。新增测试 `homeRenameOnlyTouchesTitle` 锁住这个语义。
- 标题清洗抽成 `ProjectHomeSupport.normalizedTitle`，与工作台 `TitleField` 同一口径（去首尾空白，纯空白视为无效并保持原标题），避免两处对「什么算合法标题」给出不同答案。
- 「在 Finder 中显示」的定位逻辑抽成 `finderRevealTarget(projectDirectory:baseDirectory:fileExists:)`：项目目录存在选它，否则退回根目录，都不存在时返回 nil 由调用方提示，而不是静默无反应。注入 `fileExists` 只为可测。
- 验证：**514/514，71 套件全绿**（511 → 514，新增 3 条：`normalizedTitle`、`finderRevealTarget`、`homeRenameOnlyTouchesTitle`）。`swift build` 干净。右键菜单本身需目视确认。
- **删除项目仍未实现，仍等老板单独授权**。它会真删本机项目记录与 `MeetingFileStore` 下的录音文件，不可逆，属计划第一节明令需单独确认的动作。真要做时的形状：`ProjectStoring` 补删除路径 + 二次确认弹窗 + 清理项目目录，且必须拒绝删除正在录音/处理中的项目。

---

## 2026-07-29 傍晚 · 实机反馈诊断（打包安装后首轮实测）

批次 1–6 已 commit `39ff4bf` 并 push 到 `agent/settings-final-report-ai`，用 `Scripts/make_app.sh release` 打包、稳定身份签名后覆盖安装到 `~/Applications/BangWoFenXi.app`（安装前比对过签名 Authority，与旧版一致，Keychain 凭证未失效；旧版已备份为 `BangWoFenXi.backup-before-audit-fixes-20260729.app`）。

老板实测后报了 5 项问题。诊断结论写入 `08_帮我分析_实测反馈修复计划_20260729.md`，**其中两项的真因与表面现象不一致，值得记录**。

### 「没接入 Kimi K3」的真因是 Keychain service 断链，不是模型没接

老板要求「把 key 写进去」。实际查下来 **K3 链路早就是通的**：`AIProviderConfiguration` 默认值就是 `.k3Recommended`（`k3-256k`），`AIProviderRegistry.generate` 会把它传给 Kimi 网关。代码里不缺任何东西。

真因在凭证读取路径：

- 当前代码读 service `com.zhaobo.BangWoFenXi.credentials.v2`（`KeychainService.swift:214`），本机该 service 下 **0 条记录**
- 老板的凭证在旧 service `com.zhaobo.BangWoFenXi.cloud-api-key` 下，`kimi` 与 `kimi-oauth` **两条都在**
- `legacyAdHocService` 常量早已定义（`:215`），**但全仓库零引用**——跨 service 的迁移代码从来没写

于是 `isAnalysisConfigured` 恒为 false，AI 共创、完整总结、开花整条链路静默停摆。这也解释了老板报的第 4 项（完整总结不出现）：触发代码本就存在于结束录音路径（`ProjectWorkspaceView.swift:1629`），只是被 `if isAnalysisConfigured` 挡住，且**静默跳过不给任何提示**。

教训有两条。一是当年从 ad-hoc 签名切到稳定签名身份时，为避免旧 ACL 弹窗而换了 service 名，却只留了常量没写迁移——**改存储位置必须同时写迁移，否则等于静默丢数据**。二是 `isAnalysisConfigured` 为 false 时的静默跳过，把一个凭证问题伪装成了功能缺失，用户完全无从判断。因此第 4 项即便在凭证修好后会自愈，那句提示也必须补上。

**不需要往代码或文档写任何 API Key。** 老板的 key 一直在 Keychain，只是新版本读错了地方。

### 「暂停无继续按钮 + 按暂停报错」是崩溃残留项目暴露的显示条件缺陷

截图里那个项目顶部有黄色横幅「此项目上次未正常结束（录音中）」，计时器停在 `00:00:00`——它是崩溃残留：`status` 是 `.recording`，但本次会话 `recorder.activeMeeting` 是 nil。

底部录音条只看 `meeting.status == .recording || .paused`（`:199`）就照常显示了，于是：

- 按「暂停」走到 `MeetingRecordingService.pauseRecording()` 的 `guard let activeMeeting else throw .noActiveMeeting`（`MeetingRecordingService.swift:95`）→ 报错
- 「继续」按钮其实**早就实现了**（`:1017`），只是状态永远卡在 `.recording`，那个分支进不去

所以老板看到的「没有继续按钮」并非漏做，而是残留状态让 UI 展示了一组根本无法工作的控制。修法是给录音条的显示前提加上「本次会话真的持有 recorder」，残留项目只留黄色横幅走「标记为已结束」。**不打算**把 `noActiveMeeting` 降级为静默返回——那会掩盖真实的状态机错误。

### 另外三项

- **对话框不能拖文件**：确实缺失，只有 ＋号的 `.fileImporter`。首页导入卡片有现成的拖放校验口径可循，但类型集合不同（首页音视频 / 对话框文档），不能直接复用常量。
- **回车不能发送**：设计如此，只拦 `⌘↩`。老板确认改为 ↩ 发送、⇧↩ 换行。实现时必须处理 `hasMarkedText()`——中文联想未上屏时按 ↩ 不能误发半截内容。且需先查 `StableNoteEditor` 的全部复用点，笔记编辑器不该按 ↩ 就提交。
- 老板已确认方案 A（写自动迁移代码，只读不删旧条目）与 B（⇧↩ 换行）。

### 状态边界

- 本轮**只做了诊断和计划，未改任何代码**。工作区干净，与 `origin/agent/settings-final-report-ai` 同步，HEAD `39ff4bf`。
- 测试基线仍为 **514/514，71 套件**。
- 计划分 4 批执行：A（Keychain 迁移 + 完整总结提示）→ B（录音条显示前提）→ C（回车语义）→ D（对话框拖放）。
- **打包安装未获授权**：修完须先问老板再替换 App。「删除项目」仍待单独授权，未实现。

---

## 2026-07-30 · 实测反馈修复批次 A–D（5 项全修，527 测试全绿）

按 `08_帮我分析_实测反馈修复计划_20260729.md` 执行完 4 批，每批跑一次 `Scripts/run_tests.sh`。测试 514 → 527（+13），71 套件不变。

### 批次 A：Keychain 跨 service 迁移 + 完整总结不再静默

新增 `CloudAPIKeyStore.migrateAdHocServiceCredentialsIfNeeded(from:to:)`（`KeychainService.swift`），挂在 `AppEnvironment.scheduleLegacyKeyMigration` 里、`migrateLegacyKeyIfNeeded()` **之前**——顺序不能反：先把旧 service 的值搬进来，account 级迁移才有东西可搬。

三条约束落进了代码和测试：

- **只读旧条目，绝不删除**。旧条目是老板仅有的凭证副本，且 ACL 属于另一个签名身份，删除有触发系统授权框的风险。测试里专门断言「旧 service 的分析条目/登录凭证必须原样保留」。这与同文件的 `migrateLegacyKeyIfNeeded`（会 `deleteKey()`）行为**故意不同**，改动那个函数前先看清是哪一种迁移。
- **目标已有值就不覆盖**，避免把老板新设的 key 冲掉。
- `openai` 兜底放在映射表最后，仅当 `kimi` 没被填上时才生效，不会复制没有读取方的密文。

迁移覆盖 `kimi` / `diarization` / `kimi-oauth` 三个 account，OAuth 凭证按原始 JSON 字符串搬运，测试验证搬完仍能正常解码回 `KimiOAuthTokens`。

同时补上 `ProjectWorkspaceView.finishRecording` 里 `isAnalysisConfigured == false` 的 `else` 分支：提示「AI 未连接，完整总结未生成；可前往设置连接后在『完整总结』页签手动生成」。这句话里承诺的手动入口确认存在（`startFinalReportGeneration`，未配置时会跳设置页），不是空头支票。

### 批次 B：录音条显示前提

抽出 `ProjectRuntimeSession.showsRecordBar(status:hasLiveRecorder:)` 纯函数，`ProjectWorkspaceView` 通过 `isLiveSessionActive(meeting:)` 调用，判定条件从「状态是 recording/paused」收紧为「**并且**本次会话真的持有 `recorder.activeMeeting`」。崩溃残留项目从此只剩黄色横幅走「标记为已结束」，不会再露出一组按了必然报错的控制。

**没有**放宽 `MeetingRecordingService` 的 `noActiveMeeting` 守卫——守卫报错是对的，掩盖它只会让真实的状态机错误无声消失。

### 批次 C：回车语义

新增 `NoteEditorSubmitDecision.shouldSubmit(modifiers:hasMarkedText:)` 纯函数：↩ 发送、⇧↩ 换行、⌘↩ 仍发送（保留老习惯）、⌥/⌃ 组合交回系统默认。

`hasMarkedText` 为 true 时**一律不发送**，任何修饰键都不例外——中文输入法联想未上屏时 ↩ 是「选中候选词」，此刻发送会把半截拼音当消息发出去。

查过全仓库：`StableNoteEditor` 目前**只有对话框一处使用**，笔记编辑器没有复用它，所以不需要 `submitsOnPlainReturn` 参数。若将来笔记编辑器接入，必须先加这个参数，否则会破坏多行输入。文案同步改了三处（占位符、发送按钮 help、accessibilityHelp）。

### 批次 D：对话框拖放

拖放校验放进 `ProjectAIChatAttachmentPolicy`（`Models/ProjectAIChat.swift`），**没有**复用 `ProjectHomeSupport` 的那套：首页收音视频、对话框收文档，两份类型表口径不同，共用会立刻错。`ProjectAIChatView` 的 `fileImporter` 也改为引用同一份 `referenceContentTypes`，消除两处漂移的可能。

落点在输入框区域，支持一次拖多份，`isTargeted` 高亮描边。类型不符时**同步返回 false**，系统直接给「不接受」光标，而不是先接受再弹错。＋按钮和拖放共用 `canAcceptMoreReferenceDocuments` 一道闸（发送中 / 正在读取 / 已达 4 份上限）。

### 教训：`run_tests.sh` 全绿不代表 `swift build` 能过

打包时 release 构建直接报错，才发现 debug 构建也是坏的——而我在此之前已经宣称「测试全绿、构建干净」。两个错误叠在一起：

1. **`run_tests.sh` 用 `swiftc` 手工编译，不带 Package.swift 的 Swift 6 严格并发设置**，所以它编得过、跑得过，不等于 SPM 构建能过。这个脚本存在的原因是本机 `swift test` 静默退出（见脚本头部注释），它是测试执行的唯一手段，但**不是构建门禁**。
2. 我看 `swift build` 只 `tail -5` 看尾巴，没看退出码。尾巴显示的是若干 `Compiling ...` 行，看起来正常，实际后面跟着 error。

真实问题是 `NSItemProvider` 不是 `Sendable`，在主 actor 上下文里塞进 `withTaskGroup` 的闭包会被判定为数据竞争风险（`SendingClosureRisksDataRace`）。改为顺序 `await` 取回——最多 4 个文件 URL，本来也不需要并发。

**以后每批必须同时验两道：`Scripts/run_tests.sh`（测试）＋ `swift build`（含 release，看退出码不看尾巴）。**

### 顺带确认

K3 链路复查无误：默认 `.k3Recommended`（`k3-256k`），`AIProviderRegistry` 四处传参都用 `configuration.kimiModel.modelID`。**全程没有往代码或文档写入任何 API Key。**

### 打包安装

老板授权后执行：`Scripts/make_app.sh release` → 稳定身份 `BangWoFenXi Local Code Signing` 签名（与旧版 Authority 一致，Keychain ACL 不会失效）→ 旧版备份为 `BangWoFenXi.backup-before-batchABCD-20260730.app` → `ditto` 到暂存名再 `mv` 就位（避免半拷贝状态）→ `codesign --verify --deep --strict` 通过。

安装前查过 `projects.json`：13 个项目，11 个 ready、2 个 processing（07-26 的崩溃残留，非进行中录音），无正在录音的会话，替换安全。

**一处异常，未处理，需老板知晓**：安装时有个 `BangWoFenXi` 进程（pid 44693）在跑，但它的可执行路径指向**旧备份包** `BangWoFenXi.backup-keychain-fix-20260729-011407.app`，不是已安装的 `BangWoFenXi.app`。它无窗口、0% CPU，`osascript ... to quit` 两次无响应。我**没有强杀**——强杀他人进程属不可逆动作，且它可能是之前某次调试残留。它不影响本次替换（路径不同），但下次启动前建议老板确认这个残留进程是否可以关掉。

### 状态边界

- 本轮改动已 commit（12 个源码/测试文件 + 本日志），**未 push**。
- 已打包安装并替换 `~/Applications/BangWoFenXi.app`（老板本轮明确授权）。
- 以下 5 项只能由老板目视/实操确认，测试覆盖不到：Keychain 迁移在真机上是否真的把凭证接上（首次启动后看设置页是否显示已连接）、中文输入法下 ↩ 与 ⇧↩ 的真实手感、拖放高亮与多文件、崩溃残留项目是否不再出现录音条、结束录音后完整总结是否自动出现。
- 「删除项目」仍未实现，仍等单独授权。

---

## 2026-07-30 · 收尾：删除项目、M4 恢复演练、60 分钟 soak

老板授权「把收尾工作做完」，清掉三笔挂了多轮的欠账。

### 删除项目（Bug 7 的最后一块）

「删除项目」从批次 6 起就停在授权边界上：它真删本机项目记录与录音文件，不可逆。本轮拿到授权后落地。

**为什么是永久删除，不是移到废纸篓。** 先按「更安全」的直觉去查了 `trashItem`，结论是它在这里帮倒忙：App Sandbox 下废纸篓落在容器自己的 `.Trash` 里，Finder 里看不见、用户捞不回来、磁盘也不释放——用户按删除本来就是想腾空间，给他一个打不开的回收站是假的安全网，还会让人以为「还能撤销」。所以照 V1 整场删除的既有口径做永久删除，安全感由三件真事提供：状态守卫、逐项列明的二次确认、以及录音文件独立于索引存在。

**守卫拦什么、不拦什么**（`ProjectHomeSupport.deletionBlock`）：
- 拦：此刻真在录（`liveRecordingProjectIDs` 登记过）、导入流水线正在处理这个项目。理由是这两种情况下真有进程在往项目目录里写，一边删一边写会得到半删状态。
- **不拦**：崩溃残留的 `recording`/`paused`/`processing`。一开始想按 `isAbnormalIfAppRelaunched` 一刀切拦掉，想了一下这是反的——残留的坏项目正是用户最想清掉的垃圾，拦住等于让它们永远赖在磁盘上。判断依据必须是「此刻有没有进程在写」，不是「状态字段长什么样」。
- 守卫查两次：弹确认之前查一次（不可删就别先问「确定吗」再拒绝），确认之后再查一次（弹窗挂着的时候用户可能在别处开了录音）。

**删除顺序不可颠倒**（`AppEnvironment.deleteProject`）：先摘 `projects.json` 记录，再删项目目录。反过来若中途失败会留下「有记录、无文件」的幽灵项目，点开一片空白；按现在的顺序失败只剩一个孤儿目录，用户看不见、也不会和以后的项目撞车，代价小得多。

**顺手修了一个会说假话的错误提示**：首页的 `loadError` 渲染时被硬编码加了「项目读取失败：」前缀。删除失败若复用它，会显示成「项目读取失败：这个项目正在录音，先结束录音再删除。」——一句读不通的假错误。另开了 `operationError` 承载操作类失败。

### M4 损坏恢复演练

2026-07-26 就写下的隐患：「M4 备份触发后用户会先看到空库+提示文案，人工恢复靠备份文件——实机验收时值得演练一次恢复流程。」本轮补上，脚本 `Scripts/corruption_drill.sh`。

演练在**真实 `projects.json` 的副本**上跑完整链路：健康基线（13 个项目）→ 截断成半个 JSON（最接近「写入中崩溃」的真实损坏形态）→ 读取抛 `dataCorrupted` 并带回备份文件名 → 备份保留原始损坏字节且原文件被 move 走（不是复制后留下损坏原件）→ 空库可正常重建写入 → 人工修好 JSON 改名回去 → 13 个项目全部回来 → 恢复后读写正常（写保护标志没粘住）。全部 PASS。

**安全约束**：真实文件只读复制一次，演练前后校验 SHA-256 未变（`327276dc…06dc0`，前后一致），真实目录残留损坏备份文件数 0。

演练的真正产出不是脚本而是**写进交付说明 §3.4 的人工恢复步骤**——损坏发生时老板看到的是空列表，必须知道「这是读不出来，不是已删除」，尤其不能反射性地新建项目再删旧的。也写明了录音文件在 `Meetings/<id>/` 下独立于索引存在，最坏情况索引救不回来录音仍在。

### 60 分钟完整 soak

`Scripts/soak_test.sh 3600 1`，实时速率跑满 60 分钟音频。这是交付说明 §4 第 5 / 7 条挂了多轮的「超出单次会话限制」项。**SOAK PASS**：

- 写入 158,760,960 帧 = 3600.0s 音频，读回帧数与写入完全一致（**不丢音频**）
- 提取分片 199 个，与规划数一致，**0 失败**；首个分片帧数精确等于 20 秒
- 暂停 79 段共 237,000ms，暂停期间未写文件，时间线换算自洽
- 内存 RSS 14.0 MB → 13.5 MB（**增长 −0.5 MB**，无泄漏迹象）

第 7 条「多缓冲读文件一致性」也随之落地：199 次分片提取全程读取的都是**仍在写入中**的录音文件，60 分钟真实 I/O 压力下 0 失败。

### 验证

534 测试 / 71 套件全绿（比上一轮 527 多 7 条：删除守卫 4 条、确认文案 1 条、存储层删除 2 条）。`swift build` 与 `swift build -c release` 退出码均为 0——按上一轮的教训，两道门都验，且看退出码不看输出尾巴。

### 打包安装

老板授权「存档，写日志，部署」后执行：`Scripts/make_app.sh release` → 稳定身份 `BangWoFenXi Local Code Signing` 签名（`Authority` 与被替换的那份一致，Keychain ACL 不会失效）→ 旧版备份为 `BangWoFenXi.backup-before-delete-feature-20260730.app` → `ditto` 到 `BangWoFenXi.staging.app` 并先验签，再 `mv` 就位（半拷贝的包永远不会成为线上那一份）→ `codesign --verify --deep --strict` 通过。

替换前查过：无 `BangWoFenXi` 进程在跑，13 个项目中 11 ready、2 processing（07-26 崩溃残留），**无 recording/paused**，替换安全。替换后复核 `projects.json` 仍是 677,016 字节、`sha256 327276dc…`、13 个项目——与本轮开工前完全一致，真实数据一个字节未动。

### 状态边界

- 已 commit（`7124d06`）并 push 到 `origin/agent/settings-final-report-ai`（`1482be9..7124d06`）。
- 已打包 release 并替换 `~/Applications/BangWoFenXi.app`（老板本轮明确授权）。
- **演练全程未写任何真实项目数据**：M4 演练在临时目录的副本上进行，soak 在自己的临时目录里合成音频。
- 删除功能只有自动化覆盖，**真机上的删除行为老板还没试过**——建议第一次拿一个没用的测试项目验，确认列表消失、`Meetings/<id>/` 目录也真的没了。首次删真项目前请记住：没有废纸篓可以撤销。
- 仍未做：真实客户会议的云端全链验收（需真实录音，属老板决定）。


## 2026-07-30 · 反复弹密码框的根因实测（结论：不是代码 bug，是我漏交代了一句话）

### 老板的问题

「每次打开操作，还是要输入密码是为什么，你见过哪个 APP 是这么操作的，太反人类了，我想知道，是哪条开发规则让你做了这么反人类的设计」

没有任何开发规则要求这个设计。更难看的是：`KeychainService.read()` 上原本挂着我自己写的注释——「自动读取一律禁止 SecurityAgent 弹出系统密码框」——我声明了这个意图，用了一个对老式钥匙串根本不生效的 API，然后从没验证过。

### 实测方法

写了一个探针（用完即删，不留在仓库）：**用与本 App 完全相同的签名身份和 entitlements 签名**，只用隔离的 `com.zhaobo.BangWoFenXi.probe.<UUID>` service。关键设计是读取默认传「禁止 UI」开关，于是「本来会弹框」表现为返回码或阻塞，而不是靠人眼看有没有框——比目视可靠。

第一版探针签上 `app-sandbox` 后启动即被 trap 杀掉：沙盒进程需要容器，容器需要 bundle。改成组装成 `.app` 才能跑——这一点对以后写任何沙盒探针都成立。

### 实测结果

| 场景 | 结果 |
|---|---|
| 现代钥匙串 `kSecUseDataProtectionKeychain` | `-34018` 缺 entitlement |
| 现代钥匙串 + 显式 `keychain-access-groups` | 进程被内核 **SIGKILL**（rc=137） |
| 老式：稳定身份写 → 稳定身份读 | `status=0`，静默 |
| 老式：稳定身份写 → **重新编译签名**（CDHash 变）后读 | `status=0`，**静默** |
| 老式：ad-hoc 身份写 → 稳定身份读 | **阻塞，SecurityAgent 拉起授权框** |
| 上一行的框上点「始终允许」后再静默读 ×2 | `status=0` |
| 点过「始终允许」后，**新编译的同身份**静默读 | `status=0` |

### 三个结论，其中两个推翻了我先前的说法

1. **根因不是「每次重新打包签名变了」**——我先前这么说过，第 4 行直接证伪：CDHash 变了照样静默。真正的触发条件是**写入身份与读取身份不一致**：`cloud-api-key` 那批条目是 07-21/07-24 ad-hoc 时代创建的（条目 `cdat` 可查），现在用稳定身份读，ACL 不认。
2. **`kSecUseAuthenticationUIFail` 也拦不住这个框**——我先前判断它是「文档中正确的抑制机制」，实测第 5 行传了它照样阻塞。所以老式钥匙串的 ACL 框，`LAContext.interactionNotAllowed` 和 `kSecUseAuthenticationUIFail` **两个都无效**，调用方无法抑制。
3. **「始终允许」是管用且永久的**，记的是签名身份而非二进制哈希，所以点一次之后所有新版本都免疫。老板一直被反复问，几乎肯定是点了「允许」而不是「始终允许」——**这件事的责任在我，我从头到尾没说过这个框上有两个按钮、该点哪个**。老板以为是 App 设计要反复输密码，其实是我漏了一句交代。

方案「换现代钥匙串」由第 1、2 行判定不可行（需要 Apple Team ID，自签名下声明访问组会被内核杀进程）。但结论 3 让这条路变得不必要：**不需要改代码、不需要重存凭证、不需要买开发者账号，点一次「始终允许」即可。**

### 本轮代码改动（两处，都是把话说对 + 少一个触发点）

- `KeychainService`：删掉那句假注释，把实测事实写进 `readQuery`——ACL 框抑制不了、保留 `interactionNotAllowed` 只为拦 Touch ID 那类交互、让读取静默的真正条件是签名身份一致、现代钥匙串为何走不通。避免下一个人照着一句假话做判断。
- `migrateAdHocServiceCredentialsIfNeeded`：目标条目齐全时**一次 SecItem 都不发**直接返回；有缺口时也先只查属性确认旧条目存在再取密文。启动路径上少一个可能弹框的点。

改这里时我自己引入过一个 bug：一开始用 `filter` 把待迁移列表一次算好，破坏了 openai→kimi 兜底的顺序语义（可能用旧 openai 值盖掉刚迁好的 kimi）。已改回逐轮重查，并把「为什么不能提前算」写进注释。

### 验证

535 测试 / 71 套件全绿（新增 1 条：目标齐全时不触碰旧 service），`swift build` 退出码 0。新测试做过**变异验证**——把守卫拿掉后它确实红了 3 处断言，不是永远绿的空测试。

### 状态边界

- 探针脚本已删除：它能触发授权框，不该留在仓库里。
- 老板的 4 个真实凭证条目全程只读属性、未取密文、一个都没删；探针条目残留 0。
- 改动不影响已安装的版本行为，**老板不必等发版**，下次弹框点「始终允许」即可。
- 仍未做：真实客户会议的云端全链验收（需真实录音，属老板决定）。

---

## 2026-07-30 · 09 号体验优化批次（实时总结时间标签 / 点击命名+自动声纹 / 开花实时化）

计划：`../09_帮我分析_体验优化计划_20260730.md`。老板三项需求 + 开花「无效」的断供诊断。

### 诊断中比 08 号更深一层的发现

08 号说「分析只吃 final/edited」。本轮读代码确认真实情况更糟：**「识别中」的临时片段只存在
`LocalTranscriptionController` 的 reconciler 内存里，从不进 `meeting.segments`，更不进
`project.segments`**。所以「改过滤条件」是修不了的——分析根本看不见这些片段。修法改为注入：
`ConversationAnalysisController.provisionalTailProvider`，工作台把转写控制器里最新的
provisional 尾巴（≥10 字）作为补充上下文追加进输入。

### 三条硬纪律（都有测试钉住）

1. **游标只按最终片段推进**：`analyzedThroughMs` 绝不吃尾巴的 endMs，否则尾巴定稿后落在游标
   之前永久漏分析。做过变异验证：把游标改成含尾巴，测试红（9000 ≠ 6000）。
2. **尾巴不驱动触发**：触发计数仍只数最终片段，只有尾巴时不发请求（防空转重分析）。
3. **最终分析不带尾巴**：`forceFullTranscript` 路径尾巴为 nil（完整定稿转写已覆盖）。

尾巴 id 当轮并入 `validSegmentIds`（模型可引用为证据），定稿后悬空，下一轮增量自然重写；
系统指令补了 provisional 语义：只可用于理解走向，单独作证据一律 low 置信。

### 需求 2 的拼装（零件全在，只差编排）

新纯逻辑三件套（全部可单测）：
- `SpeakerSampleWindowPlanner`：从名下片段挑最长一段，2–10s 窗口（过长取中段避开首尾抢话），
  `audioMs(forWallMs:pauseIntervals:)` 做墙钟→音频流换算（录音文件里没有暂停区间）。
- `SpeakerBackfill`：锚点指认 + 同 remoteSpeakerLabel 未识别片段批量回填。**回填不置
  edited**——这不是内容修订，云端后续更正仍应生效（与 MeetingTranscriptEditor 语义区分）。
- `SpeakerAssignPlanner.makePlan`：回填 + 窗口规划打包；已有声纹样本不覆盖。

`SpeakerMapper` 增手工指认（`assign/manualAssignments/restoreManualAssignments`），
`DiarizationController.refreshKnownSpeakers` 重建时回灌（过滤已不存在的参会人）。
声纹样本用 `AudioChunkExtractor` 从 recording.caf 切出，存 `relativeVoiceSamplePath`
标准路径——**录音进行中也能做，不再需要对麦克风单独录样本**。

UI：总结条目说话人变按钮（未识别显示「标注说话人」），弹 `SpeakerAssignSheet`
（选已有/输名新建）；转写右键菜单补「新建说话人并指认…」。锚点还是临时片段时如实拒绝
（「等它确认后再指认」）。条目 `subjectSpeakerId` 不就地改写（快照不可变纪律），
左栏立即变名，总结下一轮快照自然更新。

### 其余两项

- 需求 1：itemCard 徽章行加时间 chip，取最早证据片段 startMs，mm:ss 与左栏一致，
  点击走 onEvidenceTap 回链；证据悬空不显示。
- 需求 3-③：开花空态三分（AI 未连接 / 无对话内容 / 种子生成中），不再一句
  「还没有可开花的内容」把链路故障和内容不足混为一谈。

### 验证

550 测试 / 72 套件全绿（新增 15：SpeakerAssignFlowTests 12 + 调度器尾巴 3）；
`swift build` 0 警告；`make_app.sh` 产物签名 Authority=BangWoFenXi Local Code Signing，
与已安装版本一致。游标纪律做过变异验证（见上）。

### 状态边界

- 已打包 `build/BangWoFenXi.app`，**未安装**（授权边界：安装前必须问老板）。
- 未 commit（授权边界）。
- 实时尾巴对分析质量的实际影响需真实录音验收（临时文字有错字，模型按 low 置信处理）。
- 声纹自动提取的样本质量依赖该说话人有一段 ≥2s 的连续单人发言；
  全场都是短句抢话时 planner 返回 nil，指认仍生效，样本可去说话人面板手录。

---

## 2026-07-30 · 10 号批次（录音后 AI 全文复查转写 / 完整总结「无效」诊断与加固）

老板两条新需求：1）录音结束后 AI 再检查一遍录音、更正实时转写错误；2）完整总结功能无效。

### 「完整总结无效」的诊断：功能没坏，是慢 + 哑

查了 Vault 里的真实数据（`~/Desktop/Obsidian Vault/帮我分析/projects.json`）：
老板今天 13:29 那场录音（390 片段、84 分钟）**完整总结实际生成成功了**——
`finalReportSnapshots` 有 v1，`完整总结.md`（33KB、187 行）落盘时间 18:45，
内容质量正常。但 job 记录 `retries: 1`：**第一次生成失败过一次**。

真因链：84 分钟、390 片段的全文 + 分析账本一次性进模型，K3 生成时间逼近
240s 超时上限；一次瞬时失败（超时/网络/限流）就直接进 failed 态等人工点重试。
老板在「结束录音」后切到完整总结页签看到的是失败提示或空态，判定「无效」。
第二次（手动或共创触发的刷新）才成功——但没人告诉他后来成功了。

**修复：`FinalReportCoordinator` 瞬时错误自动重试**（超时/断网/限流/5xx，
最多 2 次，15s/30s 退避；凭证与不合规错误不重试）。测试钉住两条：
超时两次第三次成功不进失败态；invalidResponse 一次即失败不重试。
测试注入 `sleep: { _ in }` 避免真实退避等待。

### 需求 1：录音结束后 AI 全文复查转写

新 `Core/Analysis/TranscriptReviewService.swift`：
- `TranscriptReviewPrompt`：校对员人设 + 全局词库注入 + 5 条规则
  （只改有把握的同音/近音误写；wrong 必须逐字存在；宁缺毋滥；注入防护）。
- `TranscriptReviewer.makeInputJSON`：只送 final/edited，按时间排序。
  **组装留在 MainActor**（TranscriptSegment 非 Sendable，agent 只收 JSON 字符串——
  第一版直接传片段数组被 Swift 6 sending 检查拦下，这个设计是被编译器逼对的）。
- `TranscriptReviewer.apply` 本地复核四道闸：segment_id 必须存在；**edited 片段
  不改（人的修订优先于 AI）**；wrong 必须逐字在当前原文；isValidRule 防膨胀循环。
  应用后**不置 edited**——这是识别错误修复不是人工修订；复查发生在收尾
  drain 完毕之后，无云端覆盖竞争。
- 挂载点：`finishRecording` 里 `finishAndDrain` 与完整总结启动**之间**——
  总结用的是更正后的文稿。复查失败不阻断（转写保持原样，总结照常）。
- 界面新增中性提示条（noticeBanner，非红色错误条）：复查中/更正 N 处
  （带首例 wrong→right）/未发现错误/失败原因，全程可见不静默。

### 验证

558 测试 / 73 套件全绿（新增 8：转写复查 6 + coordinator 重试 2）；
`swift build` 0 警告（顺手把 `output/` 加进 Package.swift exclude，
消掉 unhandled file 警告）；重新打包，Authority 仍为稳定身份。

### 状态边界

- 已打包 `build/BangWoFenXi.app`，**未安装**（连同 09 批次一起等老板授权）。
- 未 commit。
- 复查对超长录音（如 84 分钟场次）与总结共用 240s 超时；复查本身失败不阻断，
  暂不做复查的分段与重试，等真实场次验收后再决定是否需要。
- 老板那场 13:29 的总结其实已经生成，装新版后打开该项目即可看到。

---

## 2026-08-25 · 动机分析深度框架（心理学/社会学/谈判学/沟通艺术四层透视）

老板反馈：动机板块的分析「浅薄而又表面」——prompt 缺乏心理学、社会学、谈判学
与沟通艺术的理解框架，且没有深度结合上下文与记忆。

### 诊断：模型没有工具，也没人叫它用上下文

旧 `persona` 对动机分析只有一句「对每个关键发言追问说这句话想达到什么目的」，
没有任何可调用的分析框架，模型只能输出直觉水平的判断（「他可能想要优惠」）。
更关键的是：`previous_state`（上一轮分析）和 `untrusted_user_context`（项目对话
背景补充）每次都随输入送进模型，但 prompt 从未指示模型用它们理解动机——
「结合上下文和记忆」在指令层面是缺失的，数据白白送了。

### 改动：新增 motiveFramework 常量（ConversationAnalysisPrompt.swift）

四层透视，每层给模型具体的观察抓手而不是空喊「要深刻」：

1. **心理学**：区分「追逐收益」与「回避损失」两类动机；风险不对称信号
   （对收益轻描淡写、对代价反复确认→回避型）；沉没成本成为坚持的理由；
   「你看着办」＝把决定权推出去以降低责任。
2. **社会学**：结合 speakers[].role 判断结构立场（决策者/执行者/评审者/
   利益受损方关注点天然不同）；下属用「汇报」包装「请示」、上位者用「建议」
   包装「决定」；看谁定义议程、谁反复绕回自己利益的话题。
3. **谈判学**：立场（要什么）与利益（为什么）分离；锚点、渐进让步、虚设选项、
   时间压力的识别；承诺必须落到确定行动项才可信。
4. **沟通艺术**：问题被编成「请教」还是「交办」；模糊表达与精准表达的分界处
   往往是真实意图所在；引用权威是借势；主动自我批评可能在试探反应。

**上下文融合（强制）**：每层判断都要对照 previous_state 与 untrusted_user_context
——同一话题第几次出现、此前立场与现在的差异、用户补充的身份/关系/行业背景
如何解释这句话的分量；与背景矛盾或前文无法解释的发言才作为新信息重点输出。

**红线不动**：框架内置越界说明——可描述「话里的风险不对称」「立场与利益的
距离」，但禁止声称某人「心理上恐惧/焦虑/嫉妒」或诊断人格，与 rules 第 1、7 条
对齐。`rules` 一字未改（测试断言原文包含）；不新增 category（复用
possible_motive/expression_purpose/possible_concern/stance_change/
contradiction_evasion）；JSON 合同不变。

拼装顺序：sharedGuardrails → persona → **motiveFramework** → rules → 场景规则
→ 词库。`PromptRegistry.version` 提升为 `2026-08-24.8`。

### 排查中钉住的两个坑

- **多行字符串续行符掉了半句话**：框架第一行行尾漏 `\`，导致「并在第一、四层
  强制结合上下文」与正文之间断裂。新增的防回归测试第一次跑就抓到了。
- **测试断言引用了注释而非交付字符串**：第一版断言 `不越入人格诊断与读心`——
  这句只在 /// 注释里，编译后被剥离，断言必失败。改为断言字符串内真实存在的
  「禁止声称某人」。教训：钉 prompt 的测试只能断言常量内容，不能断言注释。

### 验证

- 新增测试「动机分析深度框架进入指令且不削弱红线」（LexiconAndCorrectionTests）：
  框架进入 text()、红线原文保留、越界说明在场。
- 注入防护、红线约束、场景白名单、Schema、词库测试全部通过。
- 全量 604 例 / 75 套件：**断言零失败**；201 个环境失败全部是本轮沙箱限制
  （$TMPDIR 写权限 Code=513、Kimi 网络不可达 unexpectedStatus、AVAudio 文件
  创建被拒），与改动无关，正常环境下应全绿。

### 状态边界

- 未 commit（授权边界）；未打包未安装。
- 改动三个文件：ConversationAnalysisPrompt.swift、PromptRegistry.swift、
  LexiconAndCorrectionTests.swift。
- 实际分析质量提升需真实场次验收；指令变长约 800 字，对 K3 上下文占用可忽略。
- **设计边界已向老板说明**：目前「记忆」只到项目内（项目对话 statements +
  上一轮快照）；个人档案、Obsidian 笔记、跨项目历史不进分析输入——这是
  「完整总结不读外部知识」的有意设计。若要更强的记忆融合，需动
  ConversationAnalysisInputAssembler 输入结构，等老板拍板。
