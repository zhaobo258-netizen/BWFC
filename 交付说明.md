# 《帮我分析》Mac 内部交付说明（知识花园 A–D + 审计修复）

> 版本：阶段 A–D + H1–H4、M1–M5+L1 · 2026-07-26
> 交付口径：内部原型。代码完成、本地自动测试通过；**未签名（仅 ad-hoc）、未公证、未外发、未被客户使用**。
> 实施计划：`../01_帮我分析_Mac_MVP开发实施计划.md`

## 1. 构建、运行与测试

```bash
swift build                    # 编译（0 警告基线）
Scripts/make_app.sh            # Debug .app（build/BangWoFenXi.app，ad-hoc 签名）
Scripts/make_app.sh release    # Release .app
open build/BangWoFenXi.app     # 启动
Scripts/run_tests.sh           # 执行全部 380 个自动化用例
Scripts/soak_test.sh           # 稳定性缩短版（180s 音频 4x）
Scripts/soak_test.sh 3600 1    # 60 分钟完整稳定性（实时，约 62 分钟墙钟）
```

环境：Apple Silicon，macOS 26.5.2，Swift 6.3.2（仅 Command Line Tools，无 Xcode）。

## 2. 自动化测试覆盖（534 用例 / 71 套件，全部通过）

2026-07-30 收尾新增覆盖：删除项目的状态守卫（正在录音/导入处理中拒绝，崩溃残留允许删）、删除确认文案逐项列明、存储层删除同时清记录与项目目录。

2026-07-26 审计新增覆盖：OAuth 并发刷新单飞与 HTML 错误分类、沙盒导入授权、Project 字段所有权合并、录音写失败自动暂停、转写批量落盘、分析补发防抖、快照保留上限、项目库损坏备份与 OAuth 损坏凭证诊断。

| 领域 | 套件 | 覆盖要点 |
|---|---|---|
| Keychain | Keychain 读写服务 | 存/取/删/覆盖/多账户隔离/云端 Key 生命周期 |
| 状态机 | 会议状态机 | 7 条合法转换、全组合非法拒绝、异常退出标记、Meeting 入口 |
| 日志 | 日志脱敏 | 姓名/Key/正文不进日志、结构化字段、边界输入 |
| 持久化 | 会议持久化 | 完整模型树读写、空库、覆盖删除、内存降级、空证据无效 |
| 录音 | 录音编排 / 录音时间线 | 生命周期、失败回滚、设备拔出自动暂停、暂停区间与逆映射 |
| 文件 | 会议文件存储 | 相对路径约定、目录创建、路径逃逸拒绝、删除 |
| 回放 | 音频文件写入与回放 | 合成 PCM 写读一致、AVAudioPlayer 加载、seek 钳位、RMS |
| 表单 | 会前准备表单 / 电平评估 | 词汇、参会人 CRUD 与 4 人上限、代号复用、校验、确认时间 |
| 恢复 | 异常退出恢复 | 过滤、三种中断状态恢复到 completed、正常状态拒绝 |
| 转写 | 转写文本工具 / 转写合并器 / 本地转写控制器 | 规范化与相似度、临时→最终替换、抖动/重叠判重、20 分钟无重复模拟、上下文词汇透传、入库与持久化回调 |
| 分片 | 分片规划 / 退避策略 / 分片队列与提取 | 窗口边界、闭合产出、尾部窗口、退避序列与上限、队列持久化、提取帧数精确 |
| 识别 | 说话人映射 / 声音样本校验 | 代号映射、待识别 A/B 稳定分配、2–10s 与音量校验 |
| 云端识别 | 云端识别接口 / 云端识别编排 / multipart 构建 | URLProtocol Mock：成功/空/401/429/500/超时/坏响应/无 Key；产出/成功流/尾部/退避上限/401 暂停恢复/重启恢复/错序/只传代号 |
| 合并 | 云端片段合并 / 转写人工编辑 | 就地更新 ID 稳定、判重、人工已修订保护、说话人/文字/星标 |
| 分析 | 分析触发器 / 输入组装 / 快照构建 / 谈判分析接口 / 谈判分析调度 | 触发与防抖退避、代号化与注入防护、证据过滤、store:false 严格 schema、串行/游标/失败保留上一版/最终分析 |
| 导出 | Markdown 导出 / JSON 导出 | 7 段顺序、证据时间戳说话人、临时片段排除、往返重解析、无密钥形态 |
| 删除 | 会议删除 / finalizing 流程 | 文件树与记录清理、互不影响、beginFinish/completeFinalizing 约束 |

另：`Scripts/soak_test.sh` 无人值守稳定性（本会话已验证 180s@4x 与 240s@6x 两档 PASS：
帧数连续完整、分片提取 0 失败、暂停区间不写文件、内存增长 <1MB）。

## 3. 人工验收清单（GUI 执行，按实施计划 14.3 / 14.4）

### 3.1 Golden path

| # | 操作 | 预期结果 |
|---|---|---|
| 1 | 打开 App | 会议列表空状态；首次启动弹出麦克风权限申请 |
| 2 | 设置页粘贴 API Key 保存 | 显示「已配置」；`security find-generic-password` 外磁盘搜不到明文；连接测试按钮标注阶段 3 提供 |
| 3 | 新建谈判：填名称/背景/目标/底线/对方背景/词汇，添加 2 名参会人（姓名/阵营/角色/颜色） | 表单保存成功；参会人代号自动 p_01/p_02 |
| 4 | 为 2 名参会人各录 3–8 秒声音样本并试听 | 显示时长与「样本合格」；不足 2 秒/音量过低有明确提示 |
| 5 | 选择麦克风并「测试 5 秒」 | 显示电平条与结论；未勾告知确认时「保存并进入会中」不可用 |
| 6 | 勾选告知确认，进入会中，点「开始录音」 | 红点+计时开始；底部 3 秒内出现临时中文转写；说话人先显示「识别中」 |
| 7 | 两人交替发言约 5 分钟 | 云端确认后片段替换为带姓名最终文字；左侧出现议题结构，右侧出现六类分析卡（带明确表达/AI 推测、置信度、证据） |
| 8 | 暂停 30 秒再继续 | 计时继续；暂停区间无音频；恢复后转写与分片正常 |
| 9 | 右键某片段：改说话人、改文字、加星标 | 显示「人工已修订」；星标出现；后续云端结果不覆盖该片段 |
| 10 | 点「结束」 | 进入收尾（待处理分片完成、生成最终分析）→ 会后页面；若有失败分片，出现「重试/稍后继续处理」选择 |
| 11 | 会后：点击任一分析证据 | 底部定位并高亮对应片段，同时从该时间播放录音 |
| 12 | 会后：改一处文字后点「重新生成最终分析」 | 快照版本 +1 并替换显示 |
| 13 | 导出 Markdown / JSON | 保存面板可选位置；Markdown 可独立阅读；JSON 可被 `jq` 解析且证据 ID 有效 |
| 14 | 删除会议（二次确认） | 列明删除内容；确认后会议从列表消失，`~/Library/Application Support/BangWoFenXi/Meetings/<id>` 不存在 |
| 15 | 首页右键任一项目 → 删除项目… | 二次确认逐项列明将删内容；确认后项目从列表消失，容器内 `Meetings/<项目 id>/` 目录不存在 |
| 16 | 录音进行中右键该项目 → 删除项目… | 不弹确认，首页直接提示「这个项目正在录音，先结束录音再删除。」 |
| 17 | 删除一个「未正常结束」的崩溃残留项目 | **允许删除**（残留项目正是要清的垃圾），记录与目录一并消失 |

### 3.2 Edge cases

| # | 场景 | 预期结果 |
|---|---|---|
| 1 | 系统设置中拒绝麦克风权限后新建录音 | 阻止开始并给出系统设置入口 |
| 2 | 无 API Key | 可进本地界面；云端功能区明确「未配置」；录音/本地转写不受影响 |
| 3 | 录音中断网 3 分钟再恢复 | 顶部提示云端暂停、本地正常；分片排队；恢复后自动补传并正确落位 |
| 4 | 错误 API Key（401） | 云端模块暂停、本地继续；修复 Key 后「已修复，重试」恢复 |
| 5 | 声音样本 <2s 或 >10s | 明确不合格提示 |
| 6 | 尝试添加第 5 名参会人 | 被拒绝并提示 4 人上限 |
| 7 | 用户上翻浏览旧转写时新内容到达 | 不自动跳底；显示「回到最新」按钮 |
| 8 | 分析请求失败 | 上一版结果仍可见，不清空界面 |
| 9 | 结束会议时仍有待上传片段 | finalizing 等待；失败分片给「稍后继续处理」选择，下次启动可补传 |
| 10 | 强制退出 App 后重开 | 弹「未正常结束的会议」提示；录音文件保留；可标记已结束或查看 |
| 11 | 录音中拔出 USB 麦克风 | 不崩溃；自动暂停并提示换新设备；选新设备后可继续 |
| 12 | 会议原话中出现「忽略之前的要求」 | 分析规则不被改变（输入组装已结构隔离，自动化已验证；GUI 抽查显示效果） |

### 3.3 硬件与环境验收（14.4）

- [ ] Mac 内置麦克风，2 人面对面
- [ ] USB 全向会议麦克风，4 人围桌
- [ ] 一人距离较远（>2 米，识别下降属预期范围，需记录）
- [ ] 短时两人重叠发言（应显示低置信或待识别，不强行分配）
- [ ] 安静会议室与普通办公室噪声
- [ ] Wi-Fi 中断 3 分钟后恢复
- [x] **`Scripts/soak_test.sh 3600 1` 60 分钟完整稳定性**（2026-07-30 PASS：3600.0s 音频帧数连续、199 分片 0 失败、RSS 14.0→13.5 MB）
- [ ] 真实 API Key + 授权 2/3/4 人测试录音：分人准确率、确认延迟（目标 <30s）、分析更新（目标 <60s）
- [ ] 真实 mp3/mp4 分别经首页文件面板与拖放两条路径导入，确认检查、原件复制、提取与转写成功
- [ ] 实机制造磁盘满或录音文件不可写，确认连续写失败后自动暂停且横幅文案如实

### 3.4 项目库损坏后的人工恢复（M4 恢复流程）

`projects.json` 解码失败时，App **不会**自作主张修数据：它把原始字节改名成
`projects.corrupt-<ISO8601>.json` 留档，首页显示「数据文件损坏，已备份为 … ，原始数据未丢失」，
然后以空库继续运行。空列表是「读不出来」而不是「已删除」——此时**不要**新建项目再删旧的，
先按下面恢复。

演练命令（在真实文件的**副本**上跑完全流程，跑完校验真实文件哈希未变）：

```bash
Scripts/corruption_drill.sh
```

人工恢复步骤：

1. 打开 `~/Library/Containers/com.zhaobo.BangWoFenXi/Data/Library/Application Support/BangWoFenXi/`，
   确认存在 `projects.corrupt-<时间>.json`。**先把它复制一份到别处再动手改。**
2. 用文本编辑器修好这份 JSON。最常见的损坏是写入中断导致的尾部截断，
   表现为文件末尾缺 `]` 或最后一个项目对象不完整——删掉那半个对象、补上 `]` 即可。
3. 退出 App，把修好的文件改名为 `projects.json`（覆盖 App 重建的空库），重新打开 App。
4. 项目列表应恢复。若仍显示损坏，说明 JSON 还没修对：App 会再生成一份新的
   `projects.corrupt-…`，重复第 2 步，原始副本始终在你手上。
5. 录音文件与文稿存在 `Meetings/<项目 id>/` 下，**不受 `projects.json` 损坏影响**；
   最坏情况下即使索引救不回来，录音本身仍在磁盘上。

## 4. 已知限制与未达指标（不隐藏）

1. **SwiftData 未启用**：CLT 工具链缺少 `SwiftDataMacros` 宏插件（仅随 Xcode 分发），模型暂以纯 Swift 类实现、JSON 文件持久化（`MeetingStoring` 协议隔离）。恢复路径：安装 Xcode → 模型加回 `@Model` → 提供 SwiftData 版 `MeetingStoring`，字段零改动。
2. **XCTest 不可用**：CLT 不含 XCTest，测试统一为 swift-testing；CLT 的 `swiftpm-testing-helper` 对任何包只构建不执行，故用 `Scripts/run_tests.sh` 真实执行。安装 Xcode 后 `swift test` 可直接使用。
3. **CLI 环境麦克风授权不可用**：自动化环境无法请求麦克风权限，真实采集链路（引擎 tap → 文件 → 分析）未在硬件上验证；设备枚举已验证（本机发现 3 个输入设备）。需 GUI 首次启动授权后按 3.1/3.3 人工验证。
4. **本轮未复跑真实云端**：Kimi OAuth 刷新与 messages 网关曾于 2026-07-24 真实探针通过，但 2026-07-26 审计修复只做本地 Mock/URLProtocol 验证；分人准确率、确认延迟与真实会议分析质量仍待授权录音验收。
5. ~~**60 分钟完整稳定性待跑**~~ → **2026-07-30 已跑，SOAK PASS**：`Scripts/soak_test.sh 3600 1`，3600.0s 音频、158,760,960 帧读回一致（不丢音频）、199 个分片 0 失败、暂停 79 段 237s 换算自洽、RSS 14.0→13.5 MB（增长 −0.5 MB）。
6. **设备切换采样率限制**：更换采样率不同的麦克风后无法继续写同一文件（以 `incompatibleDeviceFormat` 拒绝继续并提示），跨格式拼接待后续处理。
7. ~~**多缓冲读文件一致性**~~ → **已由 60 分钟完整跑确认**：199 次分片提取全程读取仍在写入中的录音文件，0 失败，读回帧数与写入完全一致。
8. **Release 构建**：`make_app.sh release` 可用但自动化验证以 Debug 为主。
9. **签名与分发**：仅 ad-hoc 签名；未公证；Gatekeeper 首次打开可能需右键打开。对外交付前阻断项见实施计划 12.2（API 网关、数据协议、隐私审查、签名公证），均未开始。
10. ~~**项目库损坏后的人工恢复未验证**~~ → **2026-07-30 已演练，DRILL PASS**：`Scripts/corruption_drill.sh` 在真实 `projects.json` 副本上跑完「损坏 → 备份留档 → 空库重建 → 人工修复 → 13 个项目全部恢复」；真实文件 SHA-256 前后未变。恢复步骤见 §3.4。

## 5. 交付状态声明（按实施计划 17 节口径）

- **本地已做**：阶段 A–D 与 2026-07-26 审计工单 H1–H4、M1–M5+L1 全部代码。
- **本地已验证**：534/534 自动化用例通过（71 套件）；含真实合成 AAC 媒体探针；`swift build` 与 `swift build -c release` 退出码 0；Debug/Release `.app` 打包与稳定身份签名通过；**60 分钟实时耐久 SOAK PASS**；**M4 损坏恢复演练 DRILL PASS**。
- **远端已生效**：是（`7124d06` 已推送 `origin/agent/settings-final-report-ai`；未合并主干、未开 PR）。
- **客户可交付**：否（缺公证、隐私审查与真实云端验收）。
- **真实外发或真实使用已验证**：未外发。本机已安装（release 包已替换 `~/Applications/BangWoFenXi.app`，稳定身份签名，旧版留备份），**真机功能由老板实测确认**。
