# 《帮我分析》Mac 内部交付说明

> 版本：**v2.7.3 (19)** · 2026-09-05
> 定位：录音与对话理解的内部原型，包含独立人物库、经确认的业务记忆、轻 CRM 和回答朗读。
> 产品边界见 `../12_帮我分析_私人助理产品开发文档_20260905.md`；构建与维护要求见 `Agent.md`。

## 当前状态

| 层级 | 当前结果 |
|---|---|
| 本地已做 | AI 对话归结笔记与两项实时分析修复完成；版本 2.7.3、构建号 19；保留现有界面布局 |
| 本地已验证 | 最终 App 构建 0 警告；全量脚本实际执行 **824 例 / 89 套件通过**；稳定签名包通过验证；合成资料验证原生笔记发送、即时刷新、重开恢复及超时提示。测试编译存在既有警告 |
| 本机正式安装 | **尚未替换**，仍为 `/Applications/帮我分析.app` v2.7.2 (18)。旧版窗口控制持续超时，未能确认无活跃任务并正常退出；需完成该步骤后再备份并更新，不强退录音 |
| 源码交付 | 仓库 `zhaobo258-netizen/BWFC`，分支 `main`；提交与远端同步以本次任务回执和 Git HEAD 为准。本产品无需服务端部署 |
| 客户可交付 | 否；仍是内部原型，未公证、未对外分发，真实录音与云端能力尚待验收 |
| 真实外发或真实使用已验证 | 本轮无真实外发；合成资料上的本地 UI 通过，不代表真实业务录音和客户使用已验证 |

## v2.7.3 功能优化

- **AI 对话自动归结笔记**：一次模型回应同时返回简短笔记，标明用户补充、AI 建议或待确认事项；与回复一并原子落盘。手写正文不被覆盖，逐轮笔记不随聊天清空或消息保留上限丢失，可随项目笔记导出。未产出合法归结或保存失败时明确提示，不把整段回答伪装成笔记。笔记默认本地，供后续 AI 使用仍受项目授权控制。
- **持续发言也能触发实时分析**：按最早未消费片段计算最大等待，实时预设达到 30 秒后越过尾部防抖；仍保留单请求串行与失败退避。30 秒是调度上限，不是模型完成回应的时延承诺。
- **云端补正进入下一轮分析**：最终片段通知携带 ID，旧时间戳的文稿/说话人修正和晚到新片段重新进入增量输入；分析前同步运行时片段，持久化出错时保留原有退避。原生界面布局没有重新设计。
- **本轮验证边界**：自动归结、旧 JSON 读取、字段合并、重复保存、清空保留、切换项目晚到回应、无效模型字段、保存失败、笔记授权与导出均有测试；原生 UI 使用无网络合成回应，不能代表真实模型理解质量。正式 App 更新尚待旧版正常退出。

## v2.7.2 四项反馈修复

- **待识别人分组**：打开历史录音恢复匿名声音组，按“人物 1 / 人物 2（待识别）”显示；指认弹层明确本组条数，一次标注本组原话。没有声音标签时只标注当前一句，不凭姓名或独立分片的相同代号拼人。
- **开花**：切种子或离页取消后复位，可停止、重新开花；AI 联想先展示，来源检索随后补齐；四字原话可作为种子，合法分支不因缺少检索词而全部丢失，空结果与连接失败明确提示。
- **跨录音声纹匹配**：开录、人物设置变化及导入时刷新已登记的声音档案与人物关联；后到的可靠身份可补入匿名云端片段。导入及录音结束后使用支持的服务做一次整场识别，整场声音统一分组并匹配已启用人物；旧录音通过“识别说话人”触发，打开旧录音本身不上传音频。人工确认、请求期间的修改、已删除人物和录音均受保护。
- **动机分析**：去除套用心理先验的模板，推断卡片采用“原话依据 → 可能诉求 → 其他解释 → 待核实”；明确事实不强套四行。可能动机统一标记 AI 推断，最高中置信；增量核验回填当前有效旧原话，不用旧 AI 判断自证。

## v2.7.1 已有基础修复

- **录音基础闭环**：失败任务与异常退出状态分开呈现，重试不会误报崩溃；音频缺失时保留文稿；证据能定位原录音，合并录音按来源与暂停区间换算回放位置；写失败保留草稿，异步结果不得覆盖后来编辑或复活已删除录音。
- **独立人物身份**：无声纹也能建人；跨录音以 `Person.id` 关联；关联、合并、删除、“这是我”和改名同步到录音与相关业务数据；合并保留两侧声音附件。撤销按本次改动恢复，保留后来新增的文稿、笔记及跟进结果。
- **迁移与存储**：修复 `URL.path()` 编码路径导致中文/空格/百分号目录被误判不存在、库不回读、标记和备份失效的问题；首次迁移先备份、严格读取源数据，人物和录音保存成功后才落完成标记，失败回滚。重启不再重建已删除或已合并的人物；原 Vault 不可用时停止持久写入，避免另建分叉数据。
- **业务记忆**：候选需确认后才能进入有效记忆；确认时核验证据、人物归属、来源版本和业务作用域；失效来源需复核，拒绝项不会自动复活。手工记忆先输入再确认，不再把占位文字存成有效背景。
- **轻 CRM 与语音**：业务项目关联录音、人物和跟进；完成跟进必须填写实际结果，证据可回到文稿。回答使用系统 TTS 点击朗读，支持停止，自然结束后按钮状态复位。

## 已完成的验证及边界

最终自动化执行覆盖匿名编号恢复、整场分组、声纹刷新、人工锚点与晚到结果保护、开花取消重试、动机证据回填，以及原人物/业务/存储回归；**811 例 / 89 套件通过**。本次没有用 CLT 下仅构建不执行的 `swift test` 充当结果。

v2.7.2 在独立 Bundle ID 的原生夹具中验证了人物编号、同组两条原话一次指认、两场录音的身份显示、未配置分人服务、开花停止/重试/失败、AI 先展示，以及四行卡片窄列排版。服务使用合成音频和 Mock，不代表真实声纹准确率。

v2.7.1 的基础界面验证仍包括：

- 正常文稿与原声、证据定位、合并来源与暂停映射；空文稿和缺音频边界。
- 离线候选按指定业务项目确认，并核验确认后的状态与持久化。
- 无声纹人物创建、关联、撤销、改名及改名撤销。
- 跟进完成必须填写实际结果，来源证据跳转可用。
- TTS 自然结束复位、保存失败保留草稿、重开保留数据。

隔离 UI 使用生产视图与独立临时数据；不读取真实 Vault，也不发起真实客户转写或分析。v2.7.2 正式安装后只读打开历史录音，已看到人物编号和新入口。升级前后 8 份权威 JSON 的 SHA256 全部一致，309 个媒体文件的 inode、大小及修改时间一致。当前为 33 份录音、5 位人物、4 个声音档案。

另做了纯合成文本的真实模型探针准备：只读确认正式沙箱域中存在 Kimi OAuth 凭据，但不足本次免刷新验证窗口，因此在任何 HTTP 请求前停止（0 次请求）。没有修改生产配置；这不代表正式凭据缺失或失效，真实模型返回质量仍未验收。

本轮 v2.7.1 旧包与当前权威 JSON 已备份至 `~/Library/Application Support/帮我分析/UpgradeBackups/20260905-153652/`。更早的 v2.6.2 升级与迁移证据保留在 `20260905-125728/`；本轮没有重新迁移或重写历史数据。

## 使用与能力限制

- 旧录音须点击“识别说话人”才能重新计算整场声音组；旧分析卡片须点击共创笔记区“使用共创记录更新分析”应用新提示词。原有文字、笔记、人工归属不因打开页面而被重算。
- 自动声纹池仍最多 4 人；讯飞需要已注册 feature，选择 Provider 不等于声纹已登记。未知人物不能仅凭相同名字或相同分片编号关联。
- 整场识别目前支持讯飞与 OpenAI 兼容服务。统一转换为 16kHz/16bit/单声道 WAV 后仍受上传大小限制；超限明确停止，不拆成多个独立请求后伪称整场稳定聚类。旧稿中混合多人的粗长段若不能可靠对齐，会保留原样，可先重新转写。

1. 本轮尚未完成真实麦克风录音、真实音视频导入、Kimi/OpenAI-compatible 模型、多人声纹、外部 MCP 的现场验收。模型理解质量、识别准确率、弱收音与重叠发言表现不能从 Mock 或合成 UI 结果推断。
2. 本地转写依赖系统 Speech 能力与语言资源。不可用时可明确选择仅保存音频；这不表示文字已经生成，也不自动切换到未授权云端转写。
3. 候选提取目前限最新最多 200 个符合条件的片段、24,000 字符；每条需要独立充分的原话证据，不能宣称完整扫描任意长录音。限定业务记忆必须选择业务项目；只有原文明示且可校验的 ISO 日期才进入期限，未实现相对日期解析。
4. 人物库和业务记忆是可查看、可更正、经确认的结构化资料；当前没有自动训练模型，也没有持续监听、自主执行或专用收音/外放硬件集成。回答朗读不等于持续语音私人助理。
5. 本版本未复跑 60 分钟稳定性测试和多设备现场验收。历史模型延迟约 75–95 秒/版，尚不能宣称达到 60 秒更新目标；不同采样格式的设备切换仍可能拒绝续写同一录音。
6. 当前是 macOS 原生 SPM 应用，JSON 持久化；未启用 SwiftData。运行环境为 Apple Silicon、macOS 26.5.2、Swift 6.3.2 Command Line Tools，没有 Xcode；测试须使用项目脚本。
7. API Key/OAuth Token 按老板明确选择，明文保存在本机应用配置中，各 Provider 分开存放；运行时不调用 macOS Keychain。凭证不得写入项目、日志、导出或 Git，用户笔记仅在项目明确授权后进入相应 AI 上下文。

## 构建与本机更新

```bash
swift build
Scripts/run_tests.sh
Scripts/make_app.sh
```

打包产物使用版本后缀，当前版本为 `build/帮我分析-v2.7.3.app`。正式安装路径固定为 `/Applications/帮我分析.app`。更新前确认无活跃录音、导入或收尾任务，通过 UI 正常退出；备份旧 App 和当前权威 JSON/迁移标记，验证新包版本与稳定签名后，在同一路径原子替换并现场检查。失败时保全新数据并恢复旧包；不要通过强退、移除 quarantine 或更换 ad-hoc 签名绕过失败。具体原则见 `Agent.md`。

安装包可执行文件 SHA256：`34b049a31657d4c41bd01605dff012c7c2027d57ea70ab71ab7e3a73f6fdc134`。本轮构建/打包日志：`/tmp/bwfx-function-package.log`；全量测试日志：`/tmp/bwfx-function-final-tests.log`。临时日志只用于本机复核，不代替真实业务验收。
